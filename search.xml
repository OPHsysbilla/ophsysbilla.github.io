<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title><![CDATA[信息安全归档]]></title>
    <url>%2F2020-12-17-%E4%BF%A1%E6%81%AF%E5%AE%89%E5%85%A8%E5%BD%92%E6%A1%A3.html</url>
    <content type="text"><![CDATA[[信息安全] 开放几个课程 🚀 高性能浏览器网络(High Performance Browser Networking)英文版原文地址 TLS Security 5: Establishing a TLS Connection，acunetix是一款知名的Web网络漏洞扫描工具，它通过网络爬虫测试你的网站安全，检测流行安全漏洞的网站]]></content>
  </entry>
  <entry>
    <title><![CDATA[Jetpack 面试答案整理]]></title>
    <url>%2F2020-11-20-Jetpack-%E9%9D%A2%E8%AF%95%E7%AD%94%E6%A1%88%E6%95%B4%E7%90%86.html</url>
    <content type="text"><![CDATA[Framgnet里的setRetainInstance干嘛用的？在Acitivty因为configurationChange要销毁重建时，设置为setRetainInstance的Fragment的不会被重建，生命周期会受影响： onDestroy() 和 onCreate() 不会调用（因为fragment没有被重建） 这个setRetainInstance的Framgnet不允许在backstack里 view会被重建，但是变量不会。onCreatedView()会被调用 onDetach() 和 onAttach(Activity) 和 onActivityCreated(Bundle)还是依然会被调用的 ViewModelStore是如何存储的？在ActivityThread#performDestroyActivity的时候通过NonConfigurationInstances存储到ActivityClientRecord中，当重建时再拿出ActivityClientRecord。如果ActivityThread进程整个没了的话就没了.. 重建指当系统配置发生改变时（Configuration changes)，系统就会销毁Activity和与之关联的Fragment然后再次重建，再次调用performLaunchActivity，拿到上次onDestory的时候销毁的NonConfigurationInstances onSaveInstanceState对于大量的数据缓存有一定的局限性，大量的数据缓存则可以使用Fragment.setRetainInstance(true)来保存数据 一面1：插件化。启动activity的hook方式。taskAffity。2：okhttp支持HTTP2？http2的功能有哪些？tcp方面拥塞控制？tsl的握手和具体的非对称加密算法。非对称名称3：handler的post(Runnable)如何实现的。callback，runnable，msg的执行优先级。阻塞是怎么实现的？为什么不会阻塞主线程？5：求二叉树中两个节点之间的最大距离。6：206含义，未修改资源是哪个，302含义，301含义7：多进程通信问题。binder优势。aidl生成的java类细节。多进程遇到哪些问题？8：动态代理传入的参数都有哪些？非接口的类能实现动态代理吗？ASM的原理9：Application和Activity在Context的继承树上有何区别？二者使用上有何不同？10：任意一颗二叉树，求最大节点距离 二面1：设计一个日志系统。2：内存泄露的分类。怎么查看内存泄露的问题3：touch事件源码问题。4：组件化的问题。module和app之间的区别。moduler通信是如何实现的。5：native奔溃的日志采集，怎么处理？6：注解实现一个提示功能：如果int的值大于了3需要提示。 三面1：介绍下flutter的启动流程2：介绍下flutter与weex的区别3：组件化介绍一下4：webview中与js通信的手段有哪些？5：介绍下flutter_boost的原理]]></content>
      <categories>
        <category>面试</category>
      </categories>
      <tags>
        <tag>Android</tag>
        <tag>面试</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Linux面试答案整理]]></title>
    <url>%2F2020-11-17-Linux%E9%9D%A2%E8%AF%95%E7%AD%94%E6%A1%88%E6%95%B4%E7%90%86.html</url>
    <content type="text"><![CDATA[异步I/O 见从中断讲起的好文：epoll 的本质是什么？ 然后可以看彻底搞懂epoll高效运行的原理 linux的网络IO中是不存在异步IO的，linux的网络IO处理的第二阶段总是阻塞等待数据copy完成的。真正意义上的网络异步IO是Windows下的IOCP（IO完成端口）模型 各种I/O模型 non-blocking IO仅仅要求处理的第一阶段不block即可，而asynchronous IO要求两个阶段都不能block住 select、poll、epoll之间的区别select ，poll ，epoll 都是I/O多路复用的机制，都是同步I/O。都需要在读写事件就绪后自己负责进行读写，也就是说这个读写过程是阻塞的。好处在于可以以较少的代价来同时监听处理多个IO。 异步I/O则无需自己负责进行读写，其实现会负责把数据从内核拷贝到用户空间。epoll是Linux所特有，而select则应该是POSIX所规定，一般操作系统均有实现 select -&gt; 时间复杂度O(n) 具有O(n)的无差别轮询复杂度，同时处理的流越多无差别轮询时间就越长。 仅仅知道了有I/O事件发生了，却并不知道是哪那几个流。只能无差别轮询所有流，找出能读/写的流。 通过设置或者检查存放fd标志位的数据结构来处理。单个进程能监听的fd数量有限。 单个进程所能打开的最大连接数有FD_SETSIZE宏定义，默认1024 需要维护一个用来存放大量fd的数据结构，这样会使得用户空间和内核空间在传递该结构时复制开销大 poll -&gt; 时间复杂度O(n)poll 本质上和select没有区别。使用pollfd结构而不是select的fd_set结构 消息传递需要内核拷贝。将用户传入的数组拷贝到内核空间，查询每个fd对应的设备状态数组 如果设备就绪则在设备等待队列中加入一项并继续遍历，如果遍历完所有fd后没有发现就绪设备，则挂起当前进程。直到设备就绪或者主动超时，被唤醒后它又要再次遍历fd。这个过程经历了多次无谓的遍历。 没有最大连接数的限制，原因是它是基于链表来存储的。缺点： 大量的fd的数组被整体复制于用户态和内核地址空间之间，而不管这样的复制是不是有意义。 poll如果报告了fd后，没有被处理，那么下次poll时会再次报告该fd。 epoll -&gt; 时间复杂度O(1)epoll可以理解为event poll，不同于忙轮询和无差别轮询poll实际上是事件驱动（每个事件关联上fd）的，哪个流发生了I/O只回调哪个流 通过内核和用户空间共享一块内存来实现的。利用mmap()文件映射内存加速与内核空间的消息传递 连接数上限很大，1G内存的机器上可以打开10万左右的连接。（通过cat /proc/sys/fs/file-max可查看） epoll每次注册新的事件到epoll句柄中时（在epoll_ctl中指定EPOLL_CTL_ADD），保证了每个fd在整个过程中只会拷贝一次。 在活跃socket较少的情况下，使用epoll没有前面两者的线性下降的性能问题，但是所有socket都很活跃的情况下，可能会有性能问题 epoll有它的使用场景，更适合于处理高并发的场合。如果并发量很少，比如几百个连接这个级别，select的效率会比epoll高只有连接数大到一定长度，epoll的额外消耗才能抵消select的遍历 游戏服务器一般是one loop per thread多线程服务器此文 陈硕的 Blog 零拷贝 摘自：浅谈 Linux下的零拷贝机制 传统I/O DMA(Direct Memory Access) 直接内存访问 ：允许外设组件将I/O数据直接传送到主存储器中并且传输不需要CPU的参与，将CPU解放出来4次用户空间与内核空间的上下文切换，以及4次数据拷贝。其中4次数据拷贝中包括了2次DMA拷贝和2次CPU拷贝 通过DMA引擎将内核缓冲区中的数据传递到协议引擎(第四次拷贝: socket buffer ——&gt; protocol engine)，这次拷贝是一个独立且异步的过程（将要发送的数据放入到了一个待发送的队列中后就可以返回了）。 通过sendfile实现的零拷贝I/O3次数据拷贝中包括了2次DMA拷贝和1次CPU拷贝 支持 scatter / gather 的DMA达成非CPU拷贝2次用户空间与内核空间的上下文切换，以及2次非CPU拷贝的拷贝 没有数据拷贝到socket缓冲区，只有相应的描述符信息会被拷贝到相应的socket缓冲区当中。该描述符包含了两方面的信息：a)kernel buffer的内存地址；b)kernel buffer的偏移量 sendfile零拷贝消除了所有内核空间缓冲区与用户空间缓冲区之间的数据拷贝过程，因此应用程序无法对数据进行操作 通过mmap实现的零拷贝I/O（可以修改数据，但比sendfile昂贵）4次用户空间与内核空间的上下文切换，以及3次数据拷贝。其中3次数据拷贝中包括了2次DMA拷贝和1次CPU拷贝。 用户空间和内核空间共享这个缓冲区，不需要将数据从内核空间到用户空间的一次拷贝 用户空间可以操作缓冲区 Java 的 NIO DirectByteBuffer 里面的图是It’s all about buffers: zero-copy, mmap and Java NIO的翻译版 HeapByteBuffer ：调用ByteBuffer.allocate()时返回 JVM堆分配，被GC控制优化与回收。 不是页对齐(page aligned)的。如果要和JNI底层代码通信，JVM会拷贝这部分内容到一个页对齐的缓冲区(aligned buffer space) DirectByteBuffer ：调用ByteBuffer.allocateDirect()时返回。 底层直接malloc()，不由JVM管理，需要自动手动释放内存防止内存泄漏 页对齐(page aligned)的。可以直接和底层代码通信（例如OpenGL） MappedByteBuffer：调用FileChannel.map()时返回 是mmap()的包装类，用户空间虚拟内存和内核空间物理内存直接映射 private 模式是 copy on write， 若要修改则自己再拷贝一份到用户空间。不对其他映射相同位置的程序文件可见。 READ_WRITE 模式，unmmp写完后需要刷新 TLB 缓存和内存，修改不总是对其他相同映射对程序可见 文件描述符 在进程 A 中，文件描述符 1 和 20 都指向了同一个打开文件表项，标号为 23（指向了打开文件表中下标为 23 的数组元素），这可能是通过调用 dup()、dup2()、fcntl() 或者对同一个文件多次调用了 open() 函数形成的。 进程 A 的文件描述符 2 和进程 B 的文件描述符 2 都指向了同一个文件，这可能是在调用 fork() 后出现的（即进程 A、B 是父子进程关系），或者是不同的进程独自去调用 open() 函数打开了同一个文件，此时进程内部的描述符正好分配到与其他进程打开该文件的描述符一样。 进程 A 的描述符 0 和进程 B 的描述符 3 分别指向不同的打开文件表项，但这些表项均指向 i-node 表的同一个条目（标号为 1976）；换言之，它们指向了同一个文件。发生这种情况是因为每个进程各自对同一个文件发起了 open() 调用。同一个进程两次打开同一个文件，也会发生类似情况。 内核空间和用户空间32位操作系统下，寻址空间（虚拟地址空间，或叫线性地址空间）为 4G(2^32)。即一个进程的最大地址空间为 4G。 内核访问底层硬件设备，拥有所有权限，可以执行各种危险指令。不让普通进程使用是为了提高操作系统的稳定性及可用性。 Ring3 级别时被称为运行在用户态，而运行在 Ring0 级别时被称为运行在内核态 高字节1G为内核空间，低字节3G为各个进程使用，称为用户空间。 最高 1G 的内核空间是被所有进程共享的从用户态进入到内核态方式系统调用、软中断（定时中断等）、硬件中断 实际上每个处理器在任何指定时间点上的活动概括为下列三者之一： 运行于用户空间，执行用户进程。 运行于内核空间，处于进程上下文，代表某个特定的进程执行。 运行于内核空间，处于中断上下文，与任何进程无关，处理某个特定的中断。 为什么kill -9 杀不死一号进程init因为你无法在思考的时候关掉自己的大脑]]></content>
      <tags>
        <tag>面试</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Handler面试40问答案整理]]></title>
    <url>%2F2020-11-16-Handler%E9%9D%A2%E8%AF%9540%E9%97%AE%E7%AD%94%E6%A1%88%E6%95%B4%E7%90%86.html</url>
    <content type="text"><![CDATA[摘自 [Handler高频40问] (https://mubu.com/doc/4AOg4eG-V2v) epoll 有什么优势？Handler 和管道(Pipe)的关系？Android M 开始，将 Pipe 换成了 eventFd是为什么？Handler 底层为什么使用管道，而不是 Binder ？Handler 可以 IPC 通信吗？ 何理解 nativePollOnce() 方法？1. 最终会调用到 Native 层的 pollInner() 方法。 2. 在 pollInner() 中，处理流程： a. 先调用 epoll_wait()，这是阻塞方法，用户等待事件发生或超时。 b. 对于 epoll_wait() 返回，当且仅当一下三种情况出现时，才会返回。 POLL_ERROR，发生错误，直接跳转到 Done。 POLL_TIMEOUT，发生超时，直接跳转到 Done。 检测到管道有事件发生，则根据情况做响应的处理 如果是管道有事件发生，则直接读取管道的数据。 如果是其他事件，则处理 request，生成对应的 response 对象，push 到 response 数组。 c. 进入 Done 标记为的代码段。 先处理 Native 的 Message，调用 Native 的 Handler 来处理 Message。 再处理 Response 数组，POLL_CALLBACK 类型的事件。 d. 返回后，Java 层继续处理 Handler MessageQueue 无消息时，为什么不出现 ANRANR需要先埋下炸弹，限时时间里解除才不会导致炸弹炸毁Handler在无消息的时候是挂起的，直到下个消息来nativeWake()才接着做 应用未在规定的时间内处理 AMS 指定的任务才会 ANR。AMS 调用到应用端的 Binder 线程，应用再将任务封装成 Message 发送到主线程 Handler ，Looper.loop() 通过 MessageQueue.next() 拿到这个消息进行处理。如果不能及时处理这个消息呢，肯定是因为在它前面有耗时的消息处理，或者因为这个任务本身就很耗时。所以 ANR 不是因为 loop 循环，而是因为主线程中有耗时任务 MessageQueue 无消息时，会进入 nativePollOnce()休眠，此时无消息，处于休眠状态； MessageQueue 有消息时，会立即通过 nativeWake() 唤醒去处理消息（通过往 eventfd 发起一个写操作，这样主线程就会收到一个可读事件进而从休眠状态被唤醒） 如果 Java 层 MessageQueue 中消息很少，但是响应时间却很长，是什么原因？ MessageQueue 队列中，该 Message 前的 Message 处理较为耗时； Native 层消息过多，Java 层 MessageQueue 消息优先级最低，最后处理； 先处理Native Message，再处理Native Request，最后处理Java Message。就是因为Native有消息才用Handler+epoll的，不然只有Java的消息wait() + notify 将Pipe换成了eventFd是为什么？eventFd只用打开一个文件描述符，pipe要打开2个文件描述符，且要拷贝数据，eventFd用于通知消耗更少，完美兼容epoll逻辑，毕竟后出 Handler 分发事件优先级，是否都可拦截？拦截的优先级如何？可以统一拦截消息，但无法拦截通过Runnable通过getPostMessage(Runnable r)生成的Message。因为它msg.callback不为空会优先处理msg.callback，不会经过统一的Hanlder的mCallbackHandler的dispatchMessage函数里：1234567891011121314151617181920public void dispatchMessage(@NonNull Message msg) &#123; if (msg.callback != null) &#123; handleCallback(msg); &#125; else &#123; if (mCallback != null) &#123; // mCallback 处理完如果返回 false，还是会继续往下走，再交给 Handler.handleMessage 处理的 // 所以这边可以通过反射去 hook 一个 Handler ，可以监听 Handler 处理的每个消息，也可以改 msg 里面的值 if (mCallback.handleMessage(msg)) &#123; return; &#125; &#125; handleMessage(msg); &#125;&#125;private static Message getPostMessage(Runnable r) &#123; Message m = Message.obtain(); m.callback = r; return m;&#125; Handler 发送延迟消息，涉及哪些方法？原理是什么？会被开机时间影响吗 xxDelayed()和 xxxAtTime()，最终都会调用到 enqueueMessage() 入队从链表头按最近时间戳倒序排列，搜索直到找到一个比入队消息执行时间小的消息插入其前 不受开机时间影响，因其延迟消息基于SystemClock.uptimeMillis()，此为设备开机时间，不受时钟影响，锁屏关机的时间不会记录在内 主线程 Looper 何时运行？ActivityThread的main方法，主动调用 Looper.prepareMainLooper() 和 Looper.loop() 异步Message和Message同步屏障是什么MessageQueue是基于触发时间msg.when时间戳倒序队列（时间戳越小的，msg.when越小的，排在前面），使用同步屏障保证队列中靠后的消息会优先得到执行。一般只有Android系统能调用 同步 Message：普通 Message； 异步 Message：msg.setAsynchronous(true) UI 更新相关的消息即为异步消息，需要优先处理 同步屏障(Sync Barrier)：msg.target == null 通过Hanlder发消息的时候会持有发送的Hanlder，msg.target = this 同步屏障干嘛的同步屏障的作用是在MessageQueue中取下一个消息next()时，如果找到了target==null的同步屏障消息，需要循环遍历，一直往后找到第一个异步的消息。即阻碍同步消息，让异步消息优先 1234567891011121314151617181920212223//ViewRootImpl.java void scheduleTraversals() &#123; if (!mTraversalScheduled) &#123; mTraversalScheduled = true; //开启同步屏障 mTraversalBarrier = mHandler.getLooper().getQueue().postSyncBarrier(); //发送异步消息 mChoreographer.postCallback( Choreographer.CALLBACK_TRAVERSAL, mTraversalRunnable, null); &#125; &#125; // 用完后通过removeSyncBarrier()移除同步屏障 void unscheduleTraversals() &#123; if (mTraversalScheduled) &#123; mTraversalScheduled = false; //移除同步屏障 mHandler.getLooper().getQueue().removeSyncBarrier(mTraversalBarrier); mChoreographer.removeCallbacks( Choreographer.CALLBACK_TRAVERSAL, mTraversalRunnable, null); &#125; &#125; nextPollTimeoutMillis什么作用计算出距离上一个postDelay消息执行的，还要等待多久12345// 1.如果nextPollTimeoutMillis=-1，一直阻塞不会超时。// 2.如果nextPollTimeoutMillis=0，不会阻塞，立即返回。// 3.如果nextPollTimeoutMillis&gt;0，最长阻塞nextPollTimeoutMillis毫秒(超时)// 如果期间有程序唤醒会立即返回。int nextPollTimeoutMillis = 0; 同一个 Message 对象能否重复 send？ 角度一：Java 对象层面，可被复用；原因：Message 由消息池维护，即同一个对象被回收后会被再次复用； new Message() &amp; Message.obtain() 角度二：业务层面，不能复用；原因：Message 通过 enqueueMessage() 入队时，会通过 markInUse() 标记，再次入队无法通过 isInUse() 检查，则抛出异常；1234567891011121314151617// MessageQueue.javaboolean enqueueMessage(Message msg, long when) &#123; if (msg.target == null) &#123; throw new IllegalArgumentException("Message must have a target."); &#125; synchronized (this) &#123; if (msg.isInUse()) &#123; throw new IllegalStateException(msg + " This message is already in use."); &#125; if (mQuitting) &#123; // ... return false; &#125; msg.markInUse(); &#125;&#125; Looper 如何保证线程唯一？基于 TLS 机制，Looper 首次 prepare() 时，会将 Looper 存入 ThreadLocal 中，再次 prepare()时检查 ThreadLocal 是否已存储，如果已经有了则会抛异常，所以同一个线程只能prepare() 一次 Message 消息池的结构？最大缓存多少个 Message 对象？Message 类的静态属性 sPool 维护一个链表，最大50个享元模式，避免重复构造 Message。回收资源，回收时清理 msg 持有的 callback 和 target，避免内存泄露。 Looper 的 Printer 输出的日志，有什么其他用途？依靠的原理是什么？有什么缺点？每个Message执行开始和结束的时间会打印判断Message操作耗时，即主线程卡顿耗时，做性能监控缺点：Printer 存在大量字符串拼接，在消息量大时，会导致性能受损。在列表快速滑动时，平均帧率降低 5 帧 BlockCanary就是用此设计 子线程如何向主线程的 Handler 发送消息？为什么经过 Handler 就可以达到切线程的目的？向Handler的成员变量 MessageQueue 中添加消息 Message，下次Looper#loop()循环到的时候就可以检测到这个消息Message 主线程与工作线程之间，是共享内存地址空间的，所以是可以互相操作的，但是需要注意处理线程同步的问题。 工作线程通过主线的 Handler，就可以向其成员变量 MessageQueue 中添加 Message 主线程的WorkHandler只要拿到工作线程HandlerThread的Looper就可以通过向WorkHandler的发消息了 123456789protected void onCreate(Bundle savedInstanceState) &#123; // ... mHandlerThread = new HandlerThread("HandlerThread/Demo"); mHandlerThread.start(); mHandler = new WorkHandler(mHandlerThread.getLooper()); Message msg = mHandler.obtainMessage(); msg.obj = Thread.currentThread().getName(); mHandler.sendMessage(msg);&#125; Handler 的 IdleHandler 机制，如何理解？有什么用途IdleHandler 是 Handler 提供的一种在消息队列空闲时，执行任务的时机 执行时机是不可控的，不适合执行一些对时机要求比较高的任务。 执行的时机依赖消息队列的情况，那么如果 MessageQueue 一直有待执行的消息时，IdleHandler 就一直得不到执行 在 Looper 事件循环的过程中，当出现空闲的时候，允许我们执行任务的一种机制 出现空闲指本次Looper在取MessageQueue#next()下一条消息为空或下一条消息还没到执行时间时12345678// MessageQueue.java // If first time idle, then get the number of idlers to run. // Idle handles only run if the queue is empty or if the first message // in the queue (possibly a barrier) is due to be handled in the future. if (pendingIdleHandlerCount &lt; 0 &amp;&amp; (mMessages == null || now &lt; mMessages.when)) &#123; pendingIdleHandlerCount = mIdleHandlers.size(); &#125; IdleHandler的queueIdle() 返回值分：持续回调（true） &amp; 一次性回调（false），false 会导致执行完后，从 mIdleHandlers 中移除1234567891011try &#123; keep = idler.queueIdle(); &#125; catch (Throwable t) &#123; Log.wtf(TAG, "IdleHandler threw exception", t); &#125; if (!keep) &#123; synchronized (this) &#123; mIdleHandlers.remove(idler); &#125; &#125; IdleHandler 的 queueIdle() 返回 true，为什么不会死循环？本地变量pendingIdleHandlerCount 初始化为-1，如果有机会执行的话会置为0，所以每次MessageQueue#next()时只经过一次，本次取消息循环里不会再重复执行。下次Looper再取MessageQueue#next()时才会执行。1234567891011121314151617181920212223242526272829303132333435363738394041// MessageQueue.java Message next() &#123; int pendingIdleHandlerCount = -1; // -1 only during first iteration for (;;) &#123; // 在这里没有找到任何消息返回到此处 if (pendingIdleHandlerCount &lt; 0 &amp;&amp; (mMessages == null || now &lt; mMessages.when)) &#123; pendingIdleHandlerCount = mIdleHandlers.size(); &#125; // ... &#125; // Run the idle handlers. // We only ever reach this code block during the first iteration. for (int i = 0; i &lt; pendingIdleHandlerCount; i++) &#123; final IdleHandler idler = mPendingIdleHandlers[i]; mPendingIdleHandlers[i] = null; // release the reference to the handler boolean keep = false; try &#123; keep = idler.queueIdle(); &#125; catch (Throwable t) &#123; Log.wtf(TAG, "IdleHandler threw exception", t); &#125; if (!keep) &#123; synchronized (this) &#123; mIdleHandlers.remove(idler); &#125; &#125; &#125; // Reset the idle handler count to 0 so we do not run them again. pendingIdleHandlerCount = 0; // While calling an idle handler, a new message could have been delivered // so go back and look again for a pending message without waiting. // 执行完IdleHandler后，重新分发最近的消息，因为IdleHandler是耗时的，过程中可能有新消息来 nextPollTimeoutMillis = 0; &#125; &#125; IdleHandler 执行耗时会影响正常的消息分发吗？会，IdleHandler耗时不可控，执行时机也不可控，执行完后会重置 nextPollTimeoutMillis = 0，重新分发最近消息，因为IdleHandler是耗时的，过程中可能有新消息来 Handler 在 Activity 中使用，什么场景下会出现内存泄露？原因是什么？如何规避？主线程生命周期长于四大组件，msg.target 指向 Handler，而 Handler 作为内部类持有外部类 Activity 的引用，导致 Activity 泄露1, 静态内部类 Handler + Activity 弱引用； 随 Activity 生命周期，onDestory() 会 remove 掉所有的消息(类似Handler#removeCallbacksAndMessages())； Handler在removeMessages时为什么两次循环 因为多个handler对应同一个MessageQueue对应同一个Looper，所以一个mMessages里有多个Handler发的消息。从链表头开始删除所有 Handler h 发的对应消息时，中间可能会遇到条件不符合的其他Handler发送的消息，这时需要再开启一个循环从这个不符合条件的n的下一个消息nn开始搜。 其实问题就在于如何删除列表头？第二个循环已经足够删除所有不连续的 Handler h 发的对应消息，但无法删除链表头，因为是从链表头开始的。所有第一个循环就是干这个事的，从链表头开始删除连续的符合条件的消息1234567891011121314151617181920212223242526272829303132333435363738394041// MessageQueue.java void removeMessages(Handler h, int what, Object object) &#123; if (h == null) &#123; return; &#125; synchronized (this) &#123; Message p = mMessages; // Remove all messages at front. // 条件A：是这个Handler h 发的且what 和object对应的消息 // 从mMessages链表头开始找是不是有符合条件A的消息，并摘除 while (p != null &amp;&amp; p.target == h &amp;&amp; p.what == what &amp;&amp; (object == null || p.obj == object)) &#123; Message n = p.next; mMessages = n; // 摘除p p.recycleUnchecked(); p = n; &#125; // 此处遍历完mMessages或者遇到第一个不符合条件A的消息 // Remove all messages after front. while (p != null) &#123; // 从这个不符合条件A的消息的下一个开始找 Message n = p.next; if (n != null) &#123; // 找到符合条件A的消息 if (n.target == h &amp;&amp; n.what == what &amp;&amp; (object == null || n.obj == object)) &#123; Message nn = n.next; n.recycleUnchecked(); p.next = nn; // 删掉n continue; // 遍历n的下一个nn &#125; &#125; // 没有符合A的，不删除n，直接跳过n往后遍历 p = n; &#125; &#125; &#125; 如何理解 HandlerThread？继承 Thread，根据TLS维护子线程的 Looper，在子线程Looper#loop()将 Thread、Handler、Looper 封装，便于开发者使用 如何实现子线程等待主线程处理消息结束后，再继续执行？原理是什么？使用 Handler 的 boolean runWithScissors(final Runnable r, long timeout)实现 A 线程阻塞等待 B 线程处理完消息后再继续执行的功能。 原理很危险可能造成死锁，是@hide不允许非系统调用 如果timeout超时了，不阻塞调用线程了直接return false，但是没有取消runnable的逻辑消息还是会排队排到执行。 调用线程进入阻塞(wait())，不排队排到执行完成不会被唤醒，如果当前runnable里的代码持有别的锁，会造成死锁，本调用线程永远不执行了 1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253545556575859606162636465666768// Handler public final boolean runWithScissors(@NonNull Runnable r, long timeout) &#123; // ... // 如果同一个线程直接运行 if (Looper.myLooper() == mLooper) &#123; r.run(); return true; &#125; BlockingRunnable br = new BlockingRunnable(r); return br.postAndWait(this, timeout); &#125; private static final class BlockingRunnable implements Runnable &#123; private final Runnable mTask; private boolean mDone; public BlockingRunnable(Runnable task) &#123; mTask = task; &#125; @Override public void run() &#123; try &#123; // MessageQueue排到的时候执行 mTask.run(); &#125; finally &#123; synchronized (this) &#123; mDone = true; notifyAll(); // 唤醒阻塞 &#125; &#125; &#125; public boolean postAndWait(Handler handler, long timeout) &#123; // 入队到handler的线程 if (!handler.post(this)) &#123; return false; &#125; // 软件实现只要没完成mDone一直while阻塞在这里 synchronized (this) &#123; if (timeout &gt; 0) &#123; // 如果延时 final long expirationTime = SystemClock.uptimeMillis() + timeout; while (!mDone) &#123; long delay = expirationTime - SystemClock.uptimeMillis(); if (delay &lt;= 0) &#123; // 当超时退出时，这个 Runnable 依然还在目标线程的 MessageQueue 中，没有被移除掉，它最终还是会被 Handler 线程调度并执行。显然并不符合业务预期 return false; // timeout &#125; try &#123; wait(delay); &#125; catch (InterruptedException ex) &#123; &#125; &#125; &#125; else &#123; while (!mDone) &#123; try &#123; wait(); &#125; catch (InterruptedException ex) &#123; &#125; &#125; &#125; &#125; return true; &#125; &#125; 为什么被标记为 hide？存在什么问题？原因是什么？在子线程 Looper 中使用，可能导致 A 线程进入 wait 等待，而永远得不到被 notify 唤醒 原因： 子线程 Looper 允许退出，若包装的 BlockingRunnable 被执行前，MessageQueue 退出，则该 runnable 永远不会被执行，则会导致 A 线程一直处于 wait 等待，永远不会被 notify 唤醒 解决方法：要求该Handler的Looper使用quitSafely()不能使用quit()。quit()会清理掉所有未执行的任务。 quitSafely() 只会清理掉当前时间点之后(when &gt; now)的消息。这样runWithScissors()发送的任务，依然会被执行。]]></content>
      <categories>
        <category>面试</category>
      </categories>
      <tags>
        <tag>Android - 面试</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[暂存的工具]]></title>
    <url>%2F2020-11-13-%E6%9A%82%E5%AD%98%E7%9A%84%E5%B7%A5%E5%85%B7.html</url>
    <content type="text"><![CDATA[工具链接记录 Visual Studio Code 注意如果hexo s的时候不成功，应该是visual studio code没有保存文件… tips: VS Code缩放在设置里搜索mouseWheelZoom，然后就可以Ctrl+滚轮缩放了 类图contains a 表示整体没有了，部分也没有了 Java如何写类图可以参考[30分钟学会UML类图])(https://zhuanlan.zhihu.com/p/109655171)]]></content>
  </entry>
  <entry>
    <title><![CDATA[惹Java面试答案整理]]></title>
    <url>%2F2020-11-09-%E6%83%B9Java%E9%9D%A2%E8%AF%95%E7%AD%94%E6%A1%88%E6%95%B4%E7%90%86.html</url>
    <content type="text"><![CDATA[ThreadLocal是干嘛的，作用是什么 注解 自定义注解与元注解 APT，编译时注解处理器（Annotion Process Tool）。 获取所有该注解里的参数，可以达成生成新类的目的 插桩。自定义Gradle插件，利用Transform修改class文件 反射，运行时动态获取注解信息 getAnnotions Retrofit使用到了注解。 Retrofit使用动态代理模式实现定义的网络请求接口。 在重写invoke方法的时候构建了一个ServiceMethod对象，再使用参数的注解分析得到网络请求方式httpMethod，拼接成一个省略域名的URL12345// 定义网络请求的API接口形如：interface GithubApiService &#123; @GET("users/&#123;name&#125;/repos") Call&lt;ResponseBody&gt; searchRepoInfo(@Path("name") String name);&#125; Java IO Java中字符是采用Unicode标准，一个字符是16位 如何检测是否被Hook动态注入反射修改值？可以保留该对象的hashCode，每次调用的时候比对该对象的值 Java 双亲委派机制的破坏—SPI机制SPI(Service Provider Interface 服务提供者接口)机制在运行时才来加载具体的接口实现类，按照SPI规范指定接口实现即可加载自定义的接口实现类，实现策略模式和热拔插效果。这是一种“面向接口编程＋策略模式＋配置文件”组合实现的动态加载机制 在运行时修改资源文件可以达到加载不同实现类的功能。但是这并不是SPI的主要用法，SPI是服务提供接口，主要是给第三方用的，例如：只提供InterfaceSpi接口，不提供实现。第三方需要使用该接口时自己提供实现，然后配置在配置文件中即可。 synchronized的底层原理是什么？ Java synchronized原理总结内存可见性： 对一个变量执行lock操作，将会清空工作内存中此变量的值，在执行引擎使用这个变量前需要重新执行load或assign操作初始化变量的值 对一个变量执行unlock操作之前，必须先把此变量同步回主内存中（执行store和write操作） 锁的内存语义： 当线程释放锁时，JMM会把该线程对应的本地内存中的共享变量刷新到主内存中 当线程获取锁时，JMM会把该线程对应的本地内存置为无效。从而使得被监视器保护的临界区代码必须从主内存中读取共享变量 synchronized：原子性、可见性、有序性。 volatile不具备原子性可见性指一个线程的变量更新被其他线程所看到。需要插入一条类似load addl $0x0, (%esp)指令，变量更新值后需要将线程内存缓存刷回主内存。见内存屏障有序性通过插入内存屏障指令防止重排序，保证某些指令一定在另一些指令前完成。happen-before synchronized和ReentrantLock都是可重入锁，都是悲观锁，都是排他锁。 可重入锁：同一个线程拥有了锁仍然还可以重复申请锁。乐观锁：Java中是无锁编程的CAS算法。更新资源时先判断资源是否已经被修改，如果被修改则报错or重试。适合读操作多的场景。悲观锁：在自己用同步资源的时候一定有别人会修改它，我必须锁住同步资源。适合写操作多的场景。乐观锁是通过cmpxchg指令，去比较寄存器中的 A 和 内存中的值 V 如果相等，就把要写入的新值 B 存入内存中。 如果不相等，就将内存值 V 赋值给寄存器中的值 A。然后while循环进行重试，直到设置成功为止共享锁：读锁的共享锁可保证并发读非常高效，而读写、写读、写写的过程互斥，因为读锁和写锁是分离的。ReentrantReadWriteLock里用一个 int 32bit 高 16bit 表示读，低 16bit 表示写。除开重入的情况（同一个线程重复拿写锁，写锁+1）外，必须等写锁为0表示写锁释放后，才能拿到锁，要保证其他读线程就感知到当前写线程的操作 同步块是由monitorenter指令进入，然后monitorexit释放锁 在执行monitorenter之前需要尝试获取锁。第二个monitorexit是由编译器自动生成的，在发生异常athrow时处理异常然后释放掉锁 不可中断性。前一个不释放，后一个也一直会阻塞或者等待。所以不能用 interrupt() 中断正在获取锁的线程 中断操作Thread.interrupt()只是给线程的一个建议，最终怎么执行看线程本身的状态 若线程被中断前，该线程处于阻塞状态(调用了 wait , sleep , join 方法)，那么该线程将会立即从阻塞状态中退出，并抛出一个InterruptedException异常，同时，该线程的中断状态被设为false, 除此之外，不会发生任何事。 若线程被中断前，如果该线程处于非阻塞状态(未调用过wait,sleep,join方法)，那么该线程的中断状态将被设为true, 除此之外，不会发生任何事。interrupt()并不属于被Thread.interrupt()中断的阻塞番位，所以一个正在执行 synchronized 方法的线程被interrupt()并不会被中断抛出异常。Lock的有额外方法是可以测试中断，代码里在获取锁前测试中断状态 synchronized非公平锁。ReentrantLock可以设置公平锁。 非公平锁指会先尝试插队，插队失败再排队，可以减少唤起线程的开销，可能会饿死 线程。 公平锁缺点是吞吐率低，除第一个线程外都会阻塞，唤醒线程开销大。 公平锁在获取同步状态时，会先判断当前线程是不是第一个线程 不能知道该线程有没有拿到锁，Lock可以知道当前线程是否获取到锁 重量级锁 &gt; 轻量级锁 &gt; 偏向锁 &gt; 无锁，升级不可逆转。除非第一次JIT编译时锁消除，JVM自动消除。来自不可不说的Java“锁”事 修饰实例方法，对当前实例对象this加锁 修饰静态方法，对当前类的Class对象加锁 修饰代码块，指定一个加锁的对象，给对象加锁 synchronized锁存储在对象头的Mark Word里。存储对象的HashCode，分代年龄和锁标志位信息。 JVM堆内存中实例对象： 多出来的1行记录的是数组长度 如64位机器是Mark Word8个字节，如果对象指针Klass4个字节，对齐填充4个字节，共16字节 Mark Word里存储的数据会随着锁标志位的变化而变化，以32位的JDK为例： Mark Word里有 owner，指向当前获得锁的线程 EntryList，等待获取锁的线程 WaitList，调用wait()后释放锁的线程，需要notify来使得线程能进EntryList 偏向锁比较Thread ID，同一线程执行同步资源时自动获取。简单地测试一下对象头的Mark Word里是否存储着指向当前线程的偏向锁 只需要在置换ThreadID的时候依赖一次CAS原子指令 大多数情况下锁不存在多线程竞争且总是由同一线程获得 获取偏向锁失败，表示至少有其他线程曾竞争过偏向锁。 偏向锁不会主动释放，等到竞争出现才释放偏向锁：当到达不执行字节码的安全点时，才会暂停拥有偏向锁的线程A。 如果拥有偏向锁的线程A活着，A撤销偏向锁，升级为轻量锁，正在竞争的其他线程会进入自旋等待轻量锁。如果线程A不活跃，对象锁变为无锁状态，重新偏向 轻量级锁：在线程近乎交替执行同步块时提高性能。CAS和有限自旋，避免线程阻塞和唤醒。 在当前线程A的栈帧建立一份Lock Record，复制一份线程A要获取的锁对象Mark Word到这个空间里。 自旋锁CAS尝试改变要获取的锁对象Mark Word指向Lock Record，且Lock Record的owner指向线程A。 如果复制替换失败了，判断Mark Word是否指向当前栈帧，有说明已经获得轻量锁。如果没有，需要自适应自旋等 若自旋失败，膨胀为重量级锁。释放轻量锁的同时，唤醒被挂起的线程角逐重量级锁。 重量级锁，没有获取锁的线程阻塞等待。Mark Word中存储的是指向互斥锁的指针，等待锁的线程都会进入阻塞状态 需要系统调用，涉及用户态和内核态的转换，通过Monitor来实现线程同步，Monitor是依赖于底层的操作系统的 Mutex Lock（互斥锁）来实现的。Monitor是线程私有的数据结构，每个线程都有可用monitor record列表，同时还有一个全局的可用列表。每一个被锁住的对象都会和一个monitor关联monitor的Owner字段存放拥有该锁的线程的Thread ID notify/notifyAll方法调用后，并不会马上释放监视器锁，而是在相应的synchronized(){}/synchronized方法执行结束后才自动释放锁 什么是自旋锁自旋锁：自旋锁不会阻塞线程，只是while来让线程「等一会儿」再检查资源是否可用。 多数情况下，线程持有锁的时间都不会太长，直接挂起线程得不偿失。自旋锁是做几个空循环等待锁，通过有限的自旋次数避免因内核态切换，会自适应判断死循环直到超出阈值。 死循环是为了防止线程被挂起。异常事件和设备的中断也会发生内核态和用户态的切换。实际上用户态内核态切换耗时主要是在保存和恢复TSS任务状态段(task state segment)。 自适应自旋锁的自旋时间由前一次在同一个锁上的自旋时间及锁的拥有者的状态来决定 如何停止一个线程？只使用volatile可以吗？volatile内存屏障是什么？ 线程处于争取锁的状态时，使用Thread.interrupt()是无法中断线程的。如尝试获取synchronized锁的线程无法中断停止。 线程处于阻塞状态时，使用Thread.interrupt()方式中断该线程，抛出一个InterruptedException。中断状态将会被复位(由中断状态改为非中断状态)。 来自JavaDoc: How do I stop a thread：如果一个线程在休眠中被interrupt了，那么它的中断标记位会重置为false，并抛出一个interruptedException的异常。所以有两种最佳的处理方式： 方法里try-catch然后再进行一次interrupt，将中断标记位设置为true，这样调用的方法仍然能捕捉到中断信号。 发现中断标记位置为true后直接方法签名上直接抛出去，这样外层一层一层往出抛，最后run()里处理这个异常。 非阻塞状态的线程需要我们手动进行中断检测并结束程序。使用interrupt()更改子线程的标志位，并且在子线程的while循环里判断isInterrupted()状态 。 12345678910public void run()&#123; while(true)&#123; //判断当前线程是否被中断 if (this.isInterrupted())&#123; break; &#125; &#125; &#125; // other place ..tread.interrupt(); 非阻塞状态的线程也可以用interrupted() 12345678910public void run()&#123; try &#123; //判断当前线程是否已中断,注意interrupted方法是静态的,执行后会对中断状态进行复位 while (!Thread.interrupted()) &#123; TimeUnit.SECONDS.sleep(2); &#125; &#125; catch (InterruptedException e) &#123; &#125;&#125; 不可以单独用volatile修饰的bool标志位退出线程循环 来自JavaDoc: How do I stop a thread。同时volitale需要和synchronized同用。单独使用一个volatile修饰的bool标志位退出循环还是会有问题，当while循环里被阻塞的时候（比如BlockingQueue的put函数（使用ReenterLock）），此时线程已经阻塞住是无法走到while判断bool标志位的地方的。 volatile内存屏障是什么volatile赋值后会多执行一个load addl $0x0, (%esp)，相当于插入一个内存屏障：指令重排序时不能把后面的指令重排序到内存屏障之前的位置 读的时候从主内存而非缓存读，写的时候有任何修改要同步更新主内存12345678910111213141516┌ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┐ Main Memory│ │ ┌───────┐┌───────┐┌───────┐│ │ var A ││ var B ││ var C │ │ └───────┘└───────┘└───────┘│ │ ▲ │ ▲ │ ─ ─ ─│─│─ ─ ─ ─ ─ ─ ─ ─│─│─ ─ ─ │ │ │ │┌ ─ ─ ┼ ┼ ─ ─ ┐ ┌ ─ ─ ┼ ┼ ─ ─ ┐ ▼ │ ▼ ││ ┌───────┐ │ │ ┌───────┐ │ │ var A │ │ var C ││ └───────┘ │ │ └───────┘ │ Thread 1 Thread 2└ ─ ─ ─ ─ ─ ─ ┘ └ ─ ─ ─ ─ ─ ─ ┘ 线程池里的参数是什么意思以下内容参考Java线程池实现原理及其在美团业务中的实践 首先检测线程池运行状态，如果不是RUNNING，则直接拒绝，线程池要保证在RUNNING的状态下执行任务。 如果workerCount &lt; corePoolSize，则创建并启动一个线程来执行新提交的任务。 如果workerCount &gt;= corePoolSize，且线程池内的阻塞队列未满，则将任务添加到该阻塞队列中。 如果workerCount &gt;= corePoolSize &amp;&amp; workerCount &lt; maximumPoolSize，且线程池内的阻塞队列已满，则创建并启动一个线程来执行新提交的任务。 如果workerCount &gt;= maximumPoolSize，并且线程池内的阻塞队列已满, 则根据拒绝策略来处理该任务, 默认的处理方式是直接抛异常。 例如：线程池总大小为128（maximumPoolSize），还有一个缓冲队列（sWorkQueue，缓冲队列可以放10个任务），当尝试去添加第139个任务时程序崩溃。当线程池中的数量大于corePoolSize，缓冲队列已满，并且线程池中的数量小于maximumPoolSize，将会创建新的线程来处理被添加的任务 什么是守护进程？守护进程结束的时候一定会调用final吗守护进程需要在开启前设置JVM退出时，不必关心守护线程是否已结束，所以final可能不会被调用所有非守护线程都执行完毕后，无论有没有守护线程，虚拟机都会自动退出，但不是立马退出 注意：守护线程不能持有任何需要关闭的资源，例如打开文件等，因为虚拟机退出时，守护线程没有任何机会来关闭文件，这会导致数据丢失 你可能知道的Java知识？ clone()只拷贝第一层，只复制第一层 类比[处理器 - 缓存 - 内存]的三级层次，[线程 - 工作内存 - 主内存]。其中线程互相不可见彼此的工作内存，并通过主内存来共享交流。 volatile关键字的作用是：1、防止指令重新排序；2、保证每个线程在拿到它的那一瞬间前被刷新，拿到的是主内存中的最新值。但不能说volatile修饰了变量后就实现了线程安全：例如i++，i++这个操作本身不是原子性的。线程在拿到i时是可以保证是最新值，但是在之后加一再写回去的这两步中，其他线程可能已经修改了主内存里i的值，最终导致最后写回去的值覆盖了其他线程的操作。 Java中锁的分类有自旋锁、可重入锁、阻塞锁等等分类，其中能够造成线程卡死的锁，只有阻塞锁。 写时复制的ConcurrentHashMap的clear方法是弱一致性的，因为是不同桶结点在清理时临时加锁，所以已经被清理过的段可能会被添加新内容很正常。故现象为clear完后，里面有其他地方新加的数据。原理HashMap? ConcurrentHashMap? 相信看完这篇没人能难住你！和Java 8 ConcurrentHashMap 源码解读 1.接口与抽象类区别？2. java中的异常有哪⼏几类，分别怎么使用？ 摘自地址从根本上讲所有的异常都属于Throwable的子类，从大的方面讲分为Error（错误）和Exception（异常）。Eror是程序无法处理的异常，当发生Error时程序线程会终止运行。我们一般意义上讲的异常就是指的Exception。 3. 常⽤用的集合类有哪些？⽐如list如何排序？4. ArrayList和LinkedList内部实现大致是怎样的？他们之间的区别和优缺点？5. 内存溢出是怎么回事？举个例子。6. ==和equals的区别== 操作符比内存地址equals() 函数比值，可以被继承 7. hashCode()⽅法的作⽤一般用作Hashmap取值，重写equals()必重写hashCode()，并且保证对象为final不变更，出现存取时hashCode()不一致的问题 8. NIO是什什么？适⽤用于何种场景？9. Hashmap实现原理理？如何保证HashMap线程安全？10. jvm内存结构？为什什么需要GC？11. NIO模型，select/epoll的区别，多路复⽤的原理？12. java中⼀一个字符占多少个字节？int，long，double占多少个字节？char 2int 4long/ double / float 8 13. 创建⼀一个类的实例例都有哪些⽅方法？14. final/finaly/finalize区别？15. Session/Cookie区别？16. String/StringBuffer/StringBuilder的区别以及实现？17. Servlet⽣生命周期18. 如何⽤用java分配⼀一段连续的1G的内存空间?需要注意些什什么？19. Java有⾃己的内存回收机制，但为什么还存在内存泄漏的问题呢？20. 什什么是java序列化，如何实现java序列化（写⼀一个例例⼦子）21. String s = new String(“abc”)创建了几个String Object？如果”abc”已经存在于常量池，则只有1个；否则，2个，包含插入常量池的一个； 常量池是JDK1.8里在堆上，此前是在方法区里 new String()究竟创建几个对象? 一个new出来的一个对象new String(xx)，在堆上，内部指向常量池里的内容 深入理解Java：String 22. 静态对象：23. final关键字：24. HashMap与HashTable的区别：25. 多态：26. 集合删除：27. 参数传递与引⽤用传递：28. hash冲突：29. 在java中一个字符能否表示一个汉字：char可以表示一个汉字，char默认两个字节；Java默认UTF-16编码，一个汉字2个字节（UTF-8一个汉字3～4个字节） 30. 一致性hash：31. java反射机制32. 幂等的处理理方式：33. hashmap在jdk1.8中的改动? 冲突时大于8个元素时由链表改为了红黑树 put时是先插入，再判断是否要扩容 resize()时是尾插法，JDK1.7头插法多线程会导致环形链表Infinite Loop 扩容后新位置的计算方式变为 tab[hashCode() &amp; (length - 1)，只用计算多出来的那一位二进制位；因为扩容每次都是2倍大小，ta数组长度保证是2的幂次。JDK1.7的扩容位置计算是重新hash一遍 图文并茂的改动详情见： 美团面试题：Hashmap的结构，1.7和1.8有哪些区别，史上最深入的分析 史上最详细的 JDK 1.8 HashMap 源码解析 34. java 8 流式使⽤用：35. java域的概念： 域（Field），Java里指代的是变量，如类的静态变量和成员变量。初始化的先后顺序 36. jdk1.8中ConcurrentHashMap size⼤于8时会转化成红⿊黑树，请问有什么作⽤，如果通过remove操作，size⼩于8了了，会发⽣什么？ 红黑树可以把链表的时间复杂度O(N)操作转化为O(logN)，查找效率更高； HashMap删除元素时，红黑树树中元素小于UNTREEIFY_THRESHOLD = 6时，会进行树转为链表。static final int UNTREEIFY_THRESHOLD = 6; 37. Hashmap为什么是线程不安全的？在多线程情况下扩容会出现 CPU 接近 100%的情况吗？是因为出现了死循环吗？HashMap在put的时候，插入的元素超过了容量（由负载因子决定）的范围就会触发扩容操作，就是rehash，这个会重新将原数组的内容重新hash到新的扩容数组中，在多线程的环境下，存在同时其他的元素也在进行put操作，如果hash值相同，可能出现同时在同一数组下用链表表示，造成闭环，导致在get时会出现死循环，所以HashMap是线程不安全的 38. hashmap的计算hash值时为什么要用低 16 位与高 16 位进行异或运算用hashCode的高16位和低16位是为了减少碰撞，因为高16位一直没用到：计算下标时会&amp;一个(length - 1)的mask，&amp;操作后只有低位用到了 因为hashmap的hash函数是tab[hashCode() &amp; (length - 1)，等价于tab[ hascode() % length ]，位运算比取模运算的效率要高很多。 同时length永远为2的幂次，所以(length - 1)是一群低位为1的mask。 39. ConcurrentHashMap在JDK1.8的改动是什么？ 去掉了Segment（继承ReentrantLock，只锁部分table） 改为用synchronizied只锁住table，即每个红黑树/链表头，减少锁粒度 Segment下，每个节点都需要通过继承AQS来获得同步支持。但并不是每个节点都需要获得同步支持的，只有链表的头节点（红黑树的根节点）需要同步，这无疑带来了巨大内存浪费– 摘自ConcurrentHashMap(JDK1.8)为什么要放弃Segment Segment数组一旦初始化以后，是不可以扩容的 比如谈谈ConcurrentHashMap1.7和1.8的不同实现中有说到： size()计算方式不同 JDK1.8中使用一个volatile类型的变量baseCount记录元素的个数，当插入新数据或则删除数据时更新baseCount。 实现比1.7简单多，因为元素个数保存baseCount中，部分元素的变化个数保存在CounterCell数组中 JDK 1.7的计算方式是先不加锁，如果多次计算结果相同，则说明无修改；如果多次计算结果都不同，则给每个Segment进行加锁，再计算一次元素的个数；但是已经计算过的Segment可能会被修改 40. synchronizied是可重入锁吗？和ReentrantLock区别是什么？ ReentrantLock的锁实现是用aqs,会占用额外空间.synchronizied是底层的jvm的线程竞争. 41. String为什么避免在循环里用+拼接因为每次执行“+”操作时jvm都要new一个StringBuffer对象来处理，最后用[StringBuffer].toString()得到最终的值 42. JVM类加载机制特点1. 全盘负责， 2. 双亲委派机制，先让父类加载器试图加载该类 3. 缓存机制，一次加载后类加载存在缓存里 要判断两个类是否“相同”，就算包路径完全一致，但是加载他们的ClassLoader不一样，那么这两个类也会被认为是两个不同的类可以加载类的时候，对字节码进行修改吗？Java探针-Java Agent技术-阿里面试题JVM类加载过程，什么是双亲委派机制？ 吊打面试官-类加载器 1234567891011121314151617protected Class&lt;?&gt; loadClass(String name, boolean resolve) throws ClassNotFoundException&#123; Class&lt;?&gt; c = findLoadedClass(name); //查找是否加载过此类 if (c == null) &#123; try &#123; if (parent != null) &#123; c = parent.loadClass(name, false); //调用父类ClassLoader加载 &#125; else &#123; c = findBootstrapClassOrNull(name); //父类为null，表示为BootstrapClassLoader &#125; &#125; catch (ClassNotFoundException e) &#123; &#125; if (c == null) &#123; //父类查找为null，调用自己的查找 c = findClass(name); &#125; &#125; return c;&#125; 加载(ClassLoader的loadClass()方法) 类的全限定名(如cn.edu.hdu.test.HelloWorld.class)读取二进制字节流 将其转换为运行时数据结构(存储到方法区中) 内存中生成一个java.lang.Class对象来作为这个类的入口 连接(Linking) 验证 文件格式（是否以魔术0xCAFEBABE开头）、 元数据（这个类是否有父类）、 字节码、 符号引用验证（确保解析动作能进行） 准备为类中的所有静态变量分配内存空间，初始默认值 （static int a = 123;准备阶段时默认为0，而String b = &quot;xx&quot;; 为null ） 用final修饰的静态字段在准备阶段就被初始化为一个编译时常量表达式 解析将常量池中所有的符号引用转为直接引用（得到类或者字段、方法在内存中的指针或者偏移量，以便直接调用该方法） 这个阶段是唯一可以推迟在初始化之后再执行的，Class.loadClass()执行完获得的对象是连接后的对象。 初始化 没有静态块编译器可以不生产&lt;clinit&gt;()函数 接口与类不同的是，执行接口的&lt;clinit&gt;()方法不需要先执行父接口的&lt;clinit&gt;()方法 &lt;clinit&gt;静态变量初始化顺序由语义顺序决定 多线程调用不同类的&lt;clinit&gt;时JVM会保证线程安全，也因此会阻塞；可以利用静态内部类实现线程安全的单例模式 父类静态 - 父类构造函数 - 子类静态 - 子类构造函数 使用 卸载 该类所有的实例都已经被GC 加载该类的ClassLoader已经被GC。 该类的java.lang.Class 对象没有在任何地方被引用，包括反射 何时触发初始化 为一个类型创建一个新的对象实例时（比如new、反射、序列化） 调静态方法 赋值或读取类或接口的静态字段 用final修饰的静态字段除外，常量会在编译阶段存入调用类的常量池中 调用JavaAPI中的反射方法时（比如调用java.lang.Class中的方法，或者java.lang.reflect包中其他类的方法） 初始化一个类的派生类时（超类必须提前完成初始化操作，接口例外） JVM启动包含main方法的启动类时。 重载和重写方法调用就是指通过 .class 文件中方法的符号引用，确认方法的直接引用的过程，这个过程有可能发生在加载阶段，也有可能发生在运行阶段。 重载 （不同参数、返回值）的方法在加载阶段就确定了方法的直接引用。 有一些方法是在加载阶段就已经确定了方法的直接引用，比如：静态方法、私有方法、实例构造器方法，这类方法的调用称为 解析； 重写(Override) 的方法需要具体到对象的实际类型，所以需要特定的 Java 字节码 invokevirtual 去确定合适的方法类的生命周期是什么？加载器什么时候会被unload？类的生命周期就是从类的加载到类实例的创建与使用，再到类对象不再被使用时可以被GC卸载回收。由java虚拟机自带的三种类加载器加载的类在虚拟机的整个生命周期中是不会被卸载的，只有用户自定义的类加载器所加载的类才可以被卸载。说说Java内存回收年轻代：主要用来存放新创建的对象，年轻代分为eden区和两个Survivor区。大部分对象在Eden区中生成。当Eden区满时，还存活的对象会在两个Survivor区交替保存，达到一定次数的对象会晋升到老年代 jvm的内存模型和java的内存模型Java内存模型（Java Memory Model）与并发问题相关 保证共享内存的正确性(可见性、有序性、原子性) 缓存一致性：多线程的场景中，每个核都至少有一个L1 缓存。所有的变量都存储在主内存中，每条线程还有自己的工作内存，线程的工作内存中保存了该线程中是用到的变量的主内存副本拷贝。 volatile修改后将新值同步回主内存，在变量读取前从主内存刷新变量值 synchronized关键字保证同一时刻只允许一条线程操作，同时保证可见性、有序性、原子性，通过monitorenter和monitorexit的monitor锁来确保。 final关键字则不可修改什么是内存屏障？是为了解决什么问题？刷新缓存或禁用指令重排序指令，解决不同线程并发读写主存同一个位置时缓存一致性的问题 JVM的内存模型栈帧、操作数栈和局部变量表分别都是什么作用呢？ 基于栈的指令集系统可以很方便的做到平台无关性(x86、arm)，即使是赋值也要执行两次出栈操作。这也是为啥Java性能比C低的原因，因为操作寄存器快比操作栈快 JVM的指令集是基于栈而不是寄存器，基于栈可以具备很好的跨平台性（因为寄存器指令集往往和硬件挂钩），但缺点在于，要完成同样的操作，基于栈的实现需要更多指令才能完成（因为栈只是一个FILO结构，需要频繁压栈出栈）。另外，由于栈是在内存实现的，而寄存器是在CPU的高速缓存区，相较而言，基于栈的速度要慢很多，这也是为了跨平台性而做出的牺牲。 在编译程序代码的时候，栈帧中需要多大的局部变量表，多深的操作数栈都已经完全确定了，并且写入到方法表的Code属性中，因此一个栈帧需要多大的内存，不会受到程序运行期变量数据的影响 局部变量表 编译为Class文件时，方法的Code属性中的max_locals中确定了该方法所需分配的局部变量表的最大容量。每个表项容量(Variable Slot)为32位，64位的数据则占用2个表项。 操作数栈(Operand Stack)，一个后入先出栈(LIFO) 栈的最大深度在编译的时候写入到方法的Code属性的max_stacks数据项中 动态连接 推迟到运行期，将方法的符号引用转化为内存地址的偏移量直接引用 静态解析：类加载阶段或者第一次使用时就直接转化为直接引用，这类转化称为 方法返回地址 正常退出（没有抛出任何异常） 把当前栈帧出栈，把返回值压入调用者的操作数栈中。恢复上下文。 恢复上下文 ：包括恢复上层方法的局部变量表和操作数栈，调整PC计数器的值以指向方法调用指令后的下一条指令 异常退出 方法异常退出时，返回地址是通过异常处理器表确定的 方法正常退出时，调用者的PC计数值可以作为返回地址，栈帧中可能保存此计数值。 内存的可见性与java内存模型对原子性、可见性、有序性的保证机制；Jvm调优 常用的gc算法的特点、执行过程，和适用场景。例如g1适合对最大延迟有要求的场合，zgc适用于64为系统的大内存服务中； 常用的jvm参数，明白对不同参数的调整会有怎样的影响，适用什么样的场景。例如垃圾回收的并发数、偏向锁设置等；深入理解JVM(2)——GC算法与内存分配策略 在编程时如何合理利用栈上分配降低gc压力、如何编写适合内联优化等代码（编译方向） 线上经常full gc问题，排查过内存泄露问题（线上实际问题的排查经验） 高并发低延迟的场景，如何调整gc参数尽量降低gc停顿时间，针对队列处理机如何尽可能提高吞吐率等（特定场景的jvm优化实践或者优化） 解zgc高效的实现原理，了解Graalvm的特点（jvm最新技术）]]></content>
      <categories>
        <category>面试</category>
      </categories>
      <tags>
        <tag>面试</tag>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[计算机网络常见面试整理]]></title>
    <url>%2F2020-11-09-%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C%E5%B8%B8%E8%A7%81%E9%9D%A2%E8%AF%95%E6%95%B4%E7%90%86.html</url>
    <content type="text"><![CDATA[长连接和短连接是什么？为什么是必要的？心跳包作用、格式的定义及处理作用是保持长连接避免不断创建tcp连接报文头的定义，就是你发送数据的时候需要先发送报文头，报文里面能解析出你将要发送的数据长度你发送数据包的格式，是json的还是其他序列化的方式 Socket连接池干嘛的Socket连接池，就是维护着一定数量Socket长连接的集合。它能自动检测Socket长连接的有效性，剔除无效的连接，补充连接池的长连接的数量 http的各个版本缺陷三次握手在高延迟的场景下影响较明显，慢启动则对大量小文件请求影响较大（没有达到最大窗口请求就被终止）HTTP/1 有连接无法复用、队头阻塞、协议开销大和安全因素等多个缺陷HTTP/2 通过多路复用、二进制流与 Header 压缩等技术，极大地提高了性能，但是还是存在一些问题HTTP/3 抛弃 TCP 协议，以全新的视角重新设计 HTTP。其底层支撑是 QUIC 协议，该协议基于 UDP，有 UDP 特有的优势，同时它又取了 TCP 中的精华，实现了即快又可靠的协议 http rfc规范HTTP/2 由两个规范组成：Hypertext Transfer Protocol version 2 - RFC7540HPACK - Header Compression for HTTP/2 - RFC7541 Http加密过程是怎样的 图来自High Performance Browser Networking | O’Reilly 每一步在内容为：HTTPS中的证书认证原理和实践 The HTTPS-Only Standard 自己书写的https连接全过程 另外这篇文章有IP和TCP包头格式 让第三方CA来确保server公钥正确 1）server向CA提供基本信息、公钥等，CA用CA私钥对server公钥和信息进行加密形成数字证书（防止server被伪造）。2）client向server发出https请求和随机数c，server返回数字证书和随机数s。3）client收到数字证书，用CA公钥对数字证书进行解密，得到server的公钥。 得到server公钥后，client可以用server公钥解密server签名得到摘要。client也把原文hash一遍得到的摘要比对，确认内容没有经过更改 生成会话密钥client用server公钥加密client生成的随机数premaster secret，发送给server。server收到加密的随机数premaster secret，用server私钥解密随机数，然后和随机数c、随机数s一起生成会话密钥。此时client也拥有三个随机数生成相同的会话密钥(Session Key)。双方用会话密钥进行加密通信（防止内容被窃听进行加密） 用会话密钥(Session Key)进行加密通信是对称加密的。如DES，AES等对称加密，只有一把钥匙而获得会话密钥的过程是不对称加密的。最常见的非对称加密是RSA，有公钥和私钥，公钥加密只有私钥解密，私钥加密只有对应公钥解密 如何防止数据被篡改 数字签名是server对通信内容进行hash，得到信息摘要。 然后用server私钥对摘要进行加密得到数字签名。server把明文连同数字签名一起发送给client client用server公钥对数字签名解密得到信息摘要，然后用相同的hash对数据进行计算得到信息摘要，比对两个信息摘要是否一致。 为什么客户端在TIME_WAIT状态时还需要等2MSL后才能返回到CLOSED状态？TIME_WAIT状态的作用就是用来重发可能丢失的ACK报文。报文最大生存时间(Maximum Segment Lifetime) 1 个 MSL 确保四次挥手中主动关闭方最后的 ACK 报文最终能达到对端 1 个 MSL 确保对端没有收到 ACK 后重传的 FIN 报文可以到达 虽然双方都同意关闭连接了，但网络是不可靠的，你无法保证你最后发送的ACK报文会一定被对方收到。因此对方处于LAST_ACK状态下的Socket可能会因为超时未收到ACK报文，而重发FIN报文 TIME_WAIT状态所导致的服务器问题一台机器上端口号数量的上限是65536个，TCP服务器最大并发连接数是多少？ 如果在同一台机器上进行压力测试模拟上万的客户请求，并且循环与服务端进行短连接通信，那么这台机器将产生4000个左右的TIME_WAIT Socket，后续的短连接就会产生address already in use : connect的异常 如果使用Nginx作为方向代理也需要考虑TIME_WAIT状态，发现系统存在大量TIME_WAIT状态的连接，通过调整内核参数解决。tcp为什么是四次挥手而不是三次？因为服务端收到客户端发送的FIN后，可能还有需要发送给客户端的内容，所以只先回一个ACK如果服务端的ACK延迟到服务端发完数据后ACK和FIN一起发，会导致超过一个MSL后客户端会以为第一个客户端发起的FIN丢了，不断地重发FIN tcp为什么发起握手连接是三次，而不是两次？根本原因: 无法确认客户端的接收能力。 三次握手的目的是确认双方发送和接收的能力如果第一次客户端发起的SYN A丢包，等到客户端和服务器重新建立且正常关闭了连接后，这个丢包了的SYN A被服务器收到了，由于只需要两次，服务器会建立新连接，而客户端不知道 tcp三次握手过程中可以携带数据么？第三次握手的时候，可以携带。前两次握手不能携带数据。 如果前两次握手能够携带数据，那么一旦有人想攻击服务器，那么他只需要在第一次握手中的 SYN 报文中放大量数据，那么服务器势必会消耗更多的时间和内存空间去处理这些数据，增大了服务器被攻击的风险。 SYN Flood 攻击是什么，如何解决？典型的 DoS/DDoS 攻击 原因：客户端短时间内伪造大量不存在的IP，疯狂向服务端发送第一次握手SYN 服务端处理到大量的SYN，需要返回第二次握手的ACK和SYN，半连接队列很快被占满无法接受正常的请求 半连接队列：服务端收到以后回复第二次握手的ACK和SYN，状态由LISTEN变为SYN_RCVD，并连接挂入SYN半连接队列 由于是不存在的IP，服务端发送第二次握手的ACK和SYN后，长期收不到第三次握手，会导致服务端不断重发第二次握手 解决方法： 增加服务端SYN半连接队列容量 减少服务端重试第二次握手的次数，避免短期遇到大量的超时重发 利用SYN Cookie验证客户端存在：服务端在接收到第一次握手的SYN时不立即分配资源，根据这个SYN计算出一个Cookie，连同第二次握手回复给客户端，客户端在回复第三次握手时带上Cookie，服务端在验证Cookie合法后才会分配连接资源 介绍一下 TCP 报文头部的字段 摘自TCP 协议面试灵魂 12 问 ! 两个端口，各2字节：传输层TCP 报文怎么没有源 IP 和目标 IP 呢？这是因为在网络层 IP 层就已经处理了 IP。 序列号(Sequence number)本报文段第一个字节的序号。4字节 - [0,2^32-1]，可以循环到0 保证数据包按正确的顺序组装。 在 SYN 报文中交换彼此的初始序列号 ISN(Initial Sequence Number)三次握手时的初始序列号。ISN动态增长每4ms加一，溢出则从0重启，使得无法被攻击者猜测ISN。 ACK(Acknowledgment number)，告知对方下一个期望收到的序列号，小于ACK的所有字节都已经全部收到 SYN/ACK/FIN RST：Reset，强制断开连接 PSH：Push，对方这些数据包收到后应该马上交给上层的应用，不能缓存 窗口大小，2字节(16位)但不够用。可以有窗口缩放作为比例因子，将窗口值扩大为2^n 校验和(check sum) 2字节 可选部分： TimeStamp: TCP 时间戳。计算往返时延 RTT(Round-Trip Time)，防止序列号的回绕问题 kind = 8， length = 10， info 有两部分构成: timestamp和timestamp echo，各占 4 个字节。 计算RTT时遇到的问题：是以第一次报文为开始还是重发的为开始？所以有timestamp和timestamp echo分别记录本报文时间，和透传上一条对方发的报文的时间。 a 向 b 发送的时候，timestamp 中存放的内容就是 a 主机发送时的内核时刻 ta1。 b 向 a 回复 s2 报文的时候，timestamp 中存放的是 b 主机的时刻 tb, timestamp echo字段为从 s1 报文中解析出来的 ta1。 a 收到 b 的 s2 报文之后，此时 a 主机的内核时刻是 ta2, 而在 s2 报文中的 timestamp echo 选项中可以得到 ta1, 也就是 s2 对应的报文最初的发送时刻。然后直接采用 ta2 - ta1 就得到了 RTT 的值。 防止序列号回绕问题：网络中如果存在Seq相同报文，需要用时间戳做区分。 由于Seq溢出后会返回0，此时溢出前的Seq序列号，在溢出后从0重启后又到了相同Seq序列号。 MSS: 指的是 TCP 允许的从对方接收的最大报文段。 SACK: 选择确认选项。 Window Scale：窗口缩放选项。 TCP 的超时重传时间是如何计算的？重传间隔也叫做超时重传时间(Retransmission TimeOut, 简称RTO)，根据RTT密切变化每次传输会根据RTT更新SRTT(Smoothed round trip time，即平滑往返时间)，然后利用|RTT - SRTT|再和SRTT占不同权重求得 TCP Fast Open (TFO) 是什么？优化后的 TCP 握手流程，快速打开一个TCP连接。 在首次三次握手的后续握手，服务端验证Cookie后可以直接返回 HTTP 响应.充分利用了1 个RTT(Round-Trip Time，往返时延)的时间提前进行数据传输 可以通过上文提到的Cookie达成，后续的TCP请求客户端直接发送缓存的Cookie、SYN和HTTP请求。 服务器可以把第二次握手改为，仅验证了Cookie是否有效，就可以返回HTTP响应。客户端再第三次握手ACK 注意: 客户端最后握手的 ACK 不一定要等到服务端的 HTTP 响应到达才发送，两个过程没有任何关系。 TCP 的流量控制？接收端接受能力低，对接收端来说： SND 即send, WND 即window, UNA 即unacknowledged, 表示未被确认（已经发送未被确认），NXT 即next, 表示下一个发送的位置（未发送可以发送）上图是发送方的发送窗口。由于接收端处理能力低：大量负载来不及处理的情况，只能处理一部分，剩下的一部分留在缓冲队列里，这样下次能接受的窗口就小了。 需要发送端少发送，接收端的ACK报文首部会带上能被接受窗口大小。 此时发送端收到ACK，UNA往右移动，同时发送窗口会整个缩小 TCP 的拥塞控制？发送端发送能力低，容易丢包，对发送端来说：TCP 每条连接都需要维护两个核心状态: 拥塞窗口（Congestion Window，cwnd） 接收窗口(rwnd)是接收端给的限制；拥塞窗口(cwnd)是发送端的限制 发送窗口大小 = min(rwnd, cwnd) 慢启动阈值（Slow Start Threshold，ssthresh）涉及到的算法有这几个: 慢启动（三次握手后每一个RTT窗口大小翻倍） 拥塞避免（慢启动阈值后开始转为缓慢线性增长，避免堵塞） 快速重传和快速恢复（如果丢包，发现多个重复ACK后不用等待一个RTO，直接进行重传） 快速重传：接收端选择性重传(SACK，Selective Acknowledgment)丢掉的部分 快速恢复：（防止加重网络堵塞） cwnd和拥塞阈值同时降低为 cwnd 的一半 cwnd变为线性增长 延迟发送和延迟接收如果延迟发送和延迟接收同时一起使用，可能会造成更大的延迟，产生性能问题 如何避免小包的频繁发送？ Nagle 算法 当第一次发送数据时不用等待，就算是 1byte 的小包也立即发送 后面发送满足下面条件之一就可以发了: 数据包大小达到最大段大小(Max Segment Size, 即 MSS) 之前所有包的 ACK 都已接收到延迟确认短时间内连续收到包，可以合并ACK，除非需要立即回复ACK的场景： 接收到了大于一个 frame 的报文，且需要调整窗口大小 TCP 处于 quickack 模式（通过tcp_in_quickack_mode设置） 发现了乱序包 HTTPDNSAndroid Webview + HttpDns最佳实践DNS优化 - 百度的HTTPDNS大部分标准DNS都是基于UDP与DNS服务器交互的，HTTPDNS则是利用HTTP协议与DNS服务器交互 摘要一则：对于客户端，在PC上要学会使用tcpdump和Wireshark等工具，适当使用Fiddler和Charles等工具，很多时候电脑和手机的网络环境不见得一致，所以要在手机上使用iNetTools，Ping&amp;DNS或终端工具。学会使用工具后，要学着创造不同的网络环境，有很多工具能帮助你完成这点，比如苹果的Network Link Conditioner，FaceBook的ATC（Augmented Traffic Control）等 负载均衡nginx负载均衡调度算法默认是 round robin，也就是轮询调度算法12345678910j = i;do&#123; j = (j + 1) mod n; if (W(Sj) &gt; 0)&#123; i = j; return Si; &#125;&#125; while (j != i);return NULL; 压缩算法用的是什么？客户端发送Accept-Encoding，服务端回应123456789Accept-Encoding: gzip, compress, brAccept-Encoding: br;q=1.0, gzip;q=0.8, *;q=0.1（除了 identity 之外，都是无损压缩）gzip：表明实体采用 GNU zip 编码。（gzip 对媒体文件的压缩效果相对较差）compress：表明实体采用 Unix 的文件压缩程序。deflate：表明使用是用 zlib 的格式压缩的。br：表明实体使用 Brotli 算法的压缩格式。identity：表明没有对实体进行编码，为默认值。 具体说明见Accept-Encoding]]></content>
      <categories>
        <category>面试</category>
      </categories>
      <tags>
        <tag>面试</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Android面试答案整理]]></title>
    <url>%2F2020-11-09-Android%E9%9D%A2%E8%AF%95%E7%AD%94%E6%A1%88%E6%95%B4%E7%90%86.html</url>
    <content type="text"><![CDATA[Retrofit(动态代理)RN和flutter绘制原理Android冷启动与冷启动优化binder机制原理gradle生命周期 apk资源加载以及activitythread相关的知识，或者inputevent机制Android 系统启动流程吗？Activity的启动流程AMS启动流程预热webview；预加载离线包；点击屏幕后触发的流程讲一下，从硬件开始说。Xposed是干嘛的？ App启动流程1.首先是点击App图标，此时是运行在 Launcher 进程,通过 ActivityManagerServiceBinder IPC 的形式向 system_server 进程发起startActivity 的请求 system_server 进程接收到请求后，通过 Process#start 方法向 zygote 进程发送创建进程的请求zygote 进程 fork 出新的子进程，即 App 进程 然后进入 ActivityThread#main 方法中，这时运行在App进程中，通过 ActivityManagerServiceBinder IPC的形式向 system_server 进程发起 attachApplication 请求 获得AMS(ActivityManagerService)实例调用 mgr.attachApplication(mAppThread); system_server 接收到请求后，进行一些列准备工作后，再通过 Binder IPC 向App进程发送 scheduleLaunchActivity 请求 App进程binder线程（ ApplicationThread ）收到请求后，通过 Handler 向主线程发送 LAUNCH_ACTIVITY 消息 主线程收到Message后，通过反射机制创建目标Activity，并回调Activity的onCreate 如何计算冷启动时间？冷启动的优化是什么？apk资源加是如何加载的？热修复的资源修复的原理是什么？android 应用的启动过程分析App启动流程中 attachApplication() 方法，最终通过binder，跨进程调用到 ApplicationThread#bindApplication() 然后调用ActivityThread#handleBindApplication 方法，我们从这个方法开始看 Apk的资源是通过AssetManager.addAssetPath方法来完成加载，那么我们就可以通过反射构建自己的AssetManager对象，然后把调用addAssetPath加载自己的资源，然后把自己构建的AssetManager通过反射设置给mAssets变量，这样下次加载资源就是用的我们AssetManager，也就是用的我们更换后的资源 AndResGuard如何达到资源优化的？从R文件索引看资源优化浅谈Android中的R文件作用以及将R资源inline减少包大小关于-r-的一切 Android在构建过程中会根据资源生成R文件，里面包含了资源索引，使用该索引可以在最终生成的resources.arsc资源映射表中找到对应资源 在Library工程中引用的R资源索引不是final的。同时一个资源在两个module中重复定义了，R.txt文件中会存在多份 java编译器在编译时会将final常量进行inline内联操作，也就是App工程中的java源码编译后的class中使用的R资源索引全部会替换为常量值 最后使用的都是resources.arsc，所以R文件可以精简 可以在编译完成之后将 module 里面对于 R 的引用换成「内联」的，这样就可以少了一次内存寻址，也可以删掉被内联后的 R.class，减少了包体积又做了性能优化。 可以在编译的时候通过固定 id 来减少增删改资源带来的大量 id 变动，导致 R.java 被“连根拔起”，带来下游依赖它的 java/kotlin 文件重新编译。 stable-ids 在「热修复」、「插件化」中有很大的用处 说说组件化和插件化、热更新（热补丁）、热修复技术原理热修复与插件化都利用classloader实现加载新功能。热修复比插件化复杂，插件化只是增加新的功能或资源文件，所以不涉及抢先加载旧类的使命。热修复为了修复bug，要将新的同名类替旧的同名bug类，要抢在加载bug类之前加载新的类。 Blockcanary有什么缺点原理是替换logger的实现，塞了一个自己的，但你能塞，别人也能塞，别人在你blockcanary初始化后面塞进来就导致blockcanary失效了，同时对于dispatch中的耗时操作无法监控到，因为这个没有走handler是native调用的 组件化与插件化以下来自Android 浅谈模块化、组件化、插件化、热修复的简单理解组件化： 分成不同library，降低耦合度。 解决公共依赖关系，同一层组件不能互相引用，也不能基础引用业务的这种反向引用。 需要处理Activity的跳转问题。有路由和反射两种方式。 资源命名问题。需要让各个组件中使用统一前缀插件化： 实现原理 通过dexclassloader加载。 代理模式添加生命周期。 hook思想跳过清单验证。 宿主和插件分开编译 动态更新插件 按需下载插件 缓解65535方法数限制热修复与插件化都利用classloader实现加载新功能，热修复比插件化复杂。插件化只是增加新的功能或资源文件，所以不涉及抢先加载旧类的使命。热修复为了修复bug，要将新的同名类替旧的同名bug类，要抢在加载bug类之前加载新的类。热修复MultiDex将本地加载的dex文件插入到DexPathList中的dexElements中，后续程序运行时就会自动到该变量中去查找新的类，这就是该篇讲的热修复的原理。 热修复资源替换资源注入：资源的动态加载则相对简单。主要是参考Instant Run，通过反射调用AssetsManager的addAssets方法，将增量资源包加载到内存中来，得到新的Resources对象，然后替换掉ActivityThread等所有持有Resources的地方即可。这也是大部分热修复框架中的基本思路。 Dex分包：MultiDexAndroid Dex分包之旅含有 65536 \ LinearAlloc 等问题的解决方式Multidex分包Class依赖处理原理manifest_keep.txt脚本源码DEX分包是为了解决65536方法限制，系统在应用打包APK阶段，会将有调用关系的类打包在同一个Dex文件中，并且同一个dex中的类会被打上CLASS_ISPREVERIFIED的标志。因为加载后的类不能卸载，必须通过重启后虚拟机进行加载才能实现修复，所以此方案不支持即时生效。 QQ空间超级补丁是把BUG方法修复以后放到一个patch.dex，拿到当前应用BaseDexClassloader后，通过反射获取到DexPathList属性对象pathList、再反射调用pathList的dexElements方法把patch.dex转化为Element[]，两个Element[]进行合并，最后把patch.dex插入到dexElements数组的最前面，让虚拟机去加载修复完后的方法，就可以达到修复目的。 微信Tinker采用的是DEX差量包，整体替换DEX的方案。主要的原理是与QQ空间超级补丁技术基本相同，但不将patch.dex增加到elements数组中。差量的方式拿到patch.dex，开启新进程的服务TinkerPatchService，将patch.dex与应用中的classes.dex合并，得到一个新的fix_classess.dex。通过反射操作得到PathClassLoader的DexPatchList，再反射调用patchlist的makeDexElements()方法，把fix_classess.dex直接替换到Element[]数组中去，达到修复的目的。从而提高了兼容性和稳定性。 CLASS_ISPREVERIFIED问题如果引用者和被引用者的类(直接引用关系)在同一个Dex时,那么在虚拟机启动时，被引用类就会被打上CLASS_ISPREVERIFIED标志。这样被引用的类就不能进行热修复操作了。怎么办？ 原因是：那么我们就要阻止被引用类打上CLASS_ISPREVERIFIED标志，两个有调用关系的类不在同一个Dex文件中，那么就会抛“unexpected DEX problem”异常报错。解决办法，就是单独放一个AnitLazyLoad类在另外DEX中，在每一个类的构造方法中引用其他DEX中的唯一AnitLazyLoad类，避免类被打上CLASS_ISPREVERIFIED标志。 MultiDex如何将指定class打包到主Dex中配置一个文件，仅该文件下的内容会被打入主Dex中 模块化、组件化、插件化通讯方式不同之处 模块化相互引入，抽取了公共的common模块，其他模块自然要引入这个module。 组件化主流是隐式和路由。隐式使解耦和灵活大大降低，因此路由是主流。 插件化本身是不同进程如何加快MultiDex的速度PreMultiDex方案 安装一个新的apk的时候，先在Worker线程里做好MultiDex的解压和Optimize工作，安装apk并启动后，直接使用之前Optimize产生的odex文件，这样就可以避免第一次启动时候的Optimize工作。 缺点：第一次安装的apk没有作用，而且事先需要使用内置的apk更新功能把新版本的apk文件下载下来后，才能做PreMultiDex工作。 安装dex的时候，核心是创建DexFile对象并使用其Native方法对dex文件进行opt处理，同时生产一个与dex文件(.zip)同名的已经opt过的dex文件(.dex)。 如果安装dex的时候，这个opt过的dex文件已经存在，则跳过这个过程，这会节省许多耗时。 所以下载Apk完成的时候，预先解压dex文件，并预先触发安装dex文件以生产opt过的dex文件。 异步MultiDex方案启动App的时候，先显示一个简单的Splash闪屏界面，然后启动Worker线程执行MultiDex#install(Context)工作，就可以避免UI线程阻塞。不过要确保启动以及启动MultiDex#install(Context)所需要的类都在主dex里面(手动分包)，而且需要处理好进程同步问题。 art和darvlik区别JAVA虚拟机与Android虚拟机的区别内有PC指针变换过程 Delvik环境下，每次运行都需要通过及时编译器（JIT）将字节码转为机器码。 安装过程比较快，但程序启动每次都会重复的JIT（Just-In-Time）过程 JIT会在运行时分析应用程序的代码，识别哪些方法可以归类为热方法，这些方法会被JIT编译器编译成对应的汇编代码，然后存储到代码缓存中，以后调用这些方法时就不用解释执行了，可以直接使用代码缓存中已编译好的汇编代码。这能显著提升应用程序的执行效率 .odex是对dex的优化，deodex在系统第一次开机时会提取所有apk内的dex文件，odex优化将dex提前提取出，加快了开机的速度和程序运行的速度 .dex字节码，是针对Android设备优化后的DVM所使用的运行时编译字节码 ART（Android Runtime）通过预编译（AOT, Ahead-Of-Time）在第一次安装时将字节码转为机器码。生成OAT文件，仍以.odex保存，但是与Dalvik下不同，这个文件是可执行文件。 dex、odex 均可通过dex2oat生成oat文件，以实现兼容性以空间换时间，可能会增加10%-20%的存储空间和更长时间的应用安装在APK安装的时候就会做预先编译动作，编译好的文件是OAT文件OAT文件文件本质上是一个ELF文件，这里与dex(Odex)文件最大的区别是OAT文件不再是字节码文件，而是一个可执行文件，可以更底层的与硬件接触，运行时也省去了预编译和转译的时间 JVM 和 Darvlik VM 的区别 JVM基于栈，DVM基于寄存器 JVM一个类一个class文件，每个class冗余变量多，IO查找类时慢。Dalvik去除冗余并整合class，生成的是整个工程的一个dex文件。（如果分包的话，会有多个dex文件） class文件中包含多个不同的方法签名，如果A类文件引用B类文件中的方法，方法签名也会被复制到A类文件中（在虚拟机加载类的连接阶段将会使用该签名链接到B类的对应方法），多个不同的类会同时包含相同的方法签名 dx工具对JAVA类文件重新排列，将所有JAVA类文件中的常量池分解，消除其中的冗余信息，重新组合形成一个常量池，所有的类文件共享同一个常量池 JAVA虚拟机运行的是JAVA字节码，Dalvik虚拟机运行的是Dalvik字节码 Zygote Fork进程为什么是用socket，为什么不用binder？ fork只能拷贝当前线程，不支持多线程的fork 不建议在多线程环境使用Fork，而Binder是多线程的。怕父进程binder线程有锁，然后子进程的主线程一直在等其子线程(从父进程拷贝过来的子进程)的资源，但是其实父进程的子进程并没有被拷贝过来，造成死锁，所以fork不允许存在多线程。而非常巧的是Binder通讯偏偏就是多线程，所以干脆父进程（Zgote）这个时候就不使用binder线程 互斥锁是导致无法在多线程环境使用Fork的根本原因，大部分系统中，锁的实现基本是用户态，并非内核态（用户态更加高效）。假设在fork之前，一个线程对某个锁进行lock操作，另一个线程进行fork子进程，会导致持有锁的子线程消失了，那这个锁就会永久锁定，假如另一个线程获取该锁，就会导致死锁了。 说说组件化和插件化， 技术原理APT在编译的开始阶段对java文件进行操作，而像AscpectJ、ASM等则是在java文件编译为字节码文件后 能否在加载类的时候，对字节码进行修改?利用javaAgent和ASM字节码都可以，javaAssist还有Byte-Code都可以 Java字节码技术(二)字节码增强之ASM、JavaAssist、Agent、Instrumentation 美团 - 字节码增强技术探索包含有ASM的基本用法和工具 可否在运行时对JVM中的类进行修改并重载？：通过Instrument充当Agent + Attach API， 依赖JVMTI的Attach API机制实现 方法数越界为啥？MultiDex是怎么做的？ 方法数超过65536数会报错。 早期系统中，dexopt会把每一个类的方法id检索起来，存在一个链表结构里，而这个链表的长度是用一个short类型来保存的，2字节 = 2 ^ 16 = 2 ^ 6 2 ^ 10= 64 1024 = 64K = 65536。新版本dexopt修复了这个问题 方法数并没有超过65536，编译也完成了，但是在android2.3以前的系统安装的时候，会异常中止安装 dexopt的执行过程是在第一次加载dex文件的时候执行的。这个过程产生了一个ODEX文件，全称Optimised Dex。这个ODEX文件是在安装过程中从apk里提取出的可运行文件，是优化dex产生的，再把apk包里的dex文件删除，这样就做到了预先提取。如果没有ODEX文件，那么系统会从apk包中提取dex然后再运行。所以优化后可以加快软件的启动速度，预先提取，减少对RAM的占用。dexopt采用一个固定大小的缓冲区（LinearAlloc）来存储应用中所有方法的信息，那么之所以会出现在老版本停止安装，是因为老版本的缓冲区的大小是5M MultiDex解决方案 将APK文件中除主dex文件之外的dex文件追加到PathClassLoader（也就是BaseClassLoader）中DexPathListde Element[]数组中。这样在加载一个类的时候就会遍历所有的dex文件，保证了打包的类都能够正常加载。12345678910111213141516171819202122232425262728293031323334353637383940414243444546474849505152535455565758596061626364656667686970717273747576777879808182838485868788899091929394/** 打包时，把一个应用分成多个dex，例：classes.dex、classes2.dex、classes3.dex…，加载的时候把这些dex都追加到DexPathList对应的数组中，这样就解决了方法数的限制。Andorid 5.0之后，ART虚拟机天然支持MultiDex。Andorid 5.0之前，系统只加载一个主dex，其它的dex采用MultiDex手段来加载。*/``` &gt; 使用反射修改DexPathList。&gt; 1. 通过一定的方式把dex文件抽取出来；&gt; 2. 把这些dex文件追加到DexPathList的Element[]数组的后面；&gt; 3. 这个过程要尽可能的早，所以一般是在Application的attachBaseContext()方法中。&gt; 由于需要IO加载额外的dex文件，应用的启动速度会降低，当其他dex文件较大的时候，甚至会出现ANR现象### 如何用代码自行生成接口？之前是apt，如今是 annotationProcessor### ASM应用场景[Gradle+Transform+Asm自动化注入代码](https://www.jianshu.com/p/fffb81688dc5)[字节码插桩--你也可以轻松掌握](https://www.jianshu.com/p/13d18c631549)[【Android】函数插桩（Gradle + ASM）](https://www.jianshu.com/p/16ed4d233fd1)1. 无埋点统计、APM、插桩其实就是在上面的基础进行各种位置的插桩,具体例子[Android无埋点数据收集SDK关键技术](http://www.jianshu.com/p/b5ffe845fe2d)2. 瘦包蘑菇街的[ThinRPlugin](http://www.jianshu.com/p/b5ffe845fe2d)插件相关原理：android中的R文件，除了styleable类型外，所有字段都是int型变量/常量，且在运行期间都不会改变。所以可以在编译时，记录R中所有字段名称及对应值，然后利用asm工具遍历所有class，将引用R字段的地方替换成对应常量，然后将R$styleable.class以外的所有R.class删除掉BTW:类似瘦包的思路：Facebook redex(不是使用asm)## 知识图谱1. [Android初级、中级、高级、资深工程师(架构师、专家)技能图谱](https://www.jianshu.com/p/659381fcd4e5)2. [面试官: 说一下你做过哪些性能优化?](https://juejin.im/post/6844904105438134286)3. [面试之Android性能优化](https://www.zybuluo.com/TryLoveCatch/note/1302255)## Retrofit(动态代理)&gt; 动态代理的代理关系是在运行期确定的，Jvm帮忙生成class文件并且会删除class文件JDK原生动态代理是Java原生支持的，不需要任何外部依赖，但是它只能基于接口进行代理；CGLIB通过继承的方式进行代理，无论目标对象有没有实现接口都可以代理，但是无法处理final的情况## Binder怎么学1. [为什么Binder的通信只进行了一次拷贝](https://mubu.com/doc/explore/21079) ： 1. 基于 `mmap` 。`Client`与 `Server` 处于不同进程有着不同的虚拟地址规则，所以无法直接通信。一个页框可以映射给多个页，那么就可以将一块物理内存分别与 Client 和 Server 的虚拟内存块进行映射。映射的虚拟内存块大小将近 1M (1M-8K)，所以 IPC 通信传输的数据量也被限制为此值 &gt; —— 摘自[谈谈你对 binder 的理解？](https://zhuanlan.zhihu.com/p/143324649) 2. 通过 `copy_from_user` 将数据从用户空间拷贝到内核空间，通过 `copy_to_user` 将数据从内核空间拷贝到用户空间。一般的 IPC 方式需要分别调用这两个函数，数据就拷贝了两次，而 binder 将内核空间与目标用户空间进行了 `mmap`，只需调 `copy_from_user` 拷贝一次即可。 2. [聊聊怎样学习Binder](https://cloud.tencent.com/developer/article/1698142)内有c++代码简单传输的过程### 为什么 binder 驱动要运行在内核空间？可以移到用户空间吗？&gt; [说说你对 binder 驱动的了解？](https://zhuanlan.zhihu.com/p/152237289)binder 驱动运行在内核空间，向上层提供 /dev/binder 设备节点及 open、mmap、ioctl 等系统调用。| 结构体 | 说明 | |--|--| | binder_proc | 描述使用 binder 的进程，当调用 binder_open 函数时会创建 | | binder_thread | 描述使用 binder 的线程，当调用 binder_ioctl 函数时会创建 | | binder_node | 描述 binder 实体节点，对应于一个 server ，即用户态的 BpBinder 对象 | | binder_ref | 描述对 binder 实体节点的引用，关联到一个 binder_node | | binder_buffer | 描述 binder 通信过程中存储数据的Buffer | | binder_work | 描述一个 binder 任务 | | binder_transaction | 描述一次 binder 任务相关的数据信息 | | binder_ref_death | 描述 binder_node 即 binder server 的死亡信息 |——————————————————### oneway 是什么？如何异步等待？&gt; [看你简历上写熟悉 AIDL，说一说 oneway 吧](https://zhuanlan.zhihu.com/p/143082762)oneway 修饰的 AIDL 接口方法，是单向调用，不需要等待另一个进程的返回结果，所以方法的返回类型也只允许是 void。即应用进程只向 binder 驱动发送一次数据就结束返回，不再等待回复数据。binder 驱动会将这个请求串行排列在队列里一个个执行。1. 同步调用![同步调用](https://pic3.zhimg.com/80/v2-7b2c17503a7abea3e2cb6befaa7f603a_1440w.jpg)2. oneway的发送![oneway的发送](https://pic2.zhimg.com/80/v2-899eafe99d5c70e5cffe9edde3a9cc7d_1440w.jpg) &gt; 由外部发送给 binder 驱动的都是 BC_ 开头，由 binder 驱动发往外部的都是 BR_开头。### Intent 传值为啥会有大小限制？那跨进程传递大图怎么办？应用进程在启动 `Binder` 机制时会映射一块 1M 大小的内存，一个进程内所有正在进行的 `Binder` 事务共享这 1M 的缓冲区 。当使用 Intent 进行 IPC 时申请的缓存超过 1M ，其他事务占用的内存时，就会申请失败抛 `TransactionTooLargeException` 异常了。1. 解决方法：通过 `putBinder` 的方式传 `Bitmap` 就不会抛异常.2. 原因： `Intent` 启动组件时，系统禁掉了文件描述符 fd 机制 (`mAllowFds = false`), bitmap 无法利用共享内存，只能拷贝到 `Binder` 映射的缓冲区，导致缓冲区超限, 触发异常3. 通过 putBinder 的方式，避免了 Intent 禁用描述符的影响，bitmap 写 parcel 时的 allowFds 默认是 true , 可以利用共享内存，所以能高效传输图片。```JAVABundle b = new Bundle();b.putBinder(“binder”, new IRemoteCaller.Stub() &#123; @Override public Bitmap getBitmap() &#123; return mBitmap; &#125;&#125;);// orb.putBinder("bitmap", new ImageBinder(mBitmap)); 底部是ashmem传递的。 bundle 在IPC传递过程中是值的深拷贝，而在Fragment的setArgument之类的地方是引用传递Bitmap，本身就已经实现了 Parcelable 是可以支持序列化 Intent传输限制怎么办？ 减少传输数据量 非 ipc 通过内存共享，使用静态变量或者使用EventBus等类似的通信工具 是 ipc 可以用 file 或者 socket 文件共享（比如通过MemoryFile开辟一片共享内存，然后传递FileDescriptor，接收端用这个fd读） 匿名内存：flying-pigeon 是一个IPC 跨进程通信组件，底层是匿名内存+Binder ， 突破1MB大小限制，无需写AIDL文件，让实现跨进程通信就像写一个接口一样简单 持久化 理解AIDL中的in，out，inout吗 一文看得懂的源码：聊聊怎样学习Binder 一文超翔实的源码：Android Bander设计与实现 - 设计篇 Android ANR的产生原因，如何定位ANR 彻底理解安卓应用无响应机制 onCreate 方法里写死循环会 ANR 吗ANR 的四种场景： Service TimeOut: service 未在规定时间执行完成： 前台服务 20s，后台 200s BroadCastQueue TimeOut: 未在规定时间内未处理完广播：前台广播 10s 内, 后台 60s 内 ContentProvider TimeOut: publish 在 10s 内没有完成 Input Dispatching timeout: 5s 内未响应键盘输入、触摸屏幕等事件Activity 的生命周期回调的阻塞并不在触发 ANR 的场景里面，所以并不会直接触发 ANR。只不过死循环阻塞了主线程，如果系统再有上述的四种事件发生，就无法在相应的时间内处理从而触发 ANR Handler 40问见Handler面试40问答案整理 图片内存泄漏如何处理内存泄漏是指没有使用的对象资源与GC-Root保持可达路径，导致系统无法进行回收。 使用软引用引用Bitmap，Bitmap在确定不调用后需要recycle()，然后设置为null 分辨率大的图片需要等比例缩小 图片占用内存大小如何计算12345Bitmap.Config ARGB_8888：由4个8位组成，即A=8，R=8，G=8，B=8，那么一个像素点占8+8+8+8=32位（4字节）Bitmap.Config ARGB_4444：由4个4位组成，即A=4，R=4，G=4，B=4，那么一个像素点占4+4+4+4=16位 （2字节）Bitmap.Config RGB_565：没有透明度，R=5，G=6，B=5，，那么一个像素点占5+6+5=16位（2字节）Bitmap.Config ALPHA_8：每个像素占8位，只有透明度，没有颜色。一个像素的位数总和越高，图像也就越逼真。 假设有一张480x800的图片 在色彩模式为ARGB_8888的情况下，一个像素所占的大小为4字节，会占用 480*800*4/1024KB=1500KB 的内存； 而在RGB_565的情况下，占用的内存为：480*800*2/1024KB=750KB OkHttp相关断点续传 客户端请求 Range: bytes=200-1000 见Range 服务器返回 206 Partial Content 标示Content-Range: bytes 200-1000/67589 同时服务器响应的header中有Accept-Ranges: bytes 使用RandomAccessFile允许自由定义文件记录指针，在同一个文件的不同位置进行并发读写 客户端头的 If-Range 需要和 Range 配合起来使用，否则会被服务端忽略 以下摘自 图解：HTTP 范围请求，助力断点续传、多线程下载的核心原理： 范围请求需要 HTTP/1.1 及之上支持 客户端通过在请求头中添加 Range 这个请求头，来指定请求的内容实体的字节范围。 服务端通过 Content-Range 来标识当前返回的内容实体范围，并使用 Content-Length 来标识当前返回的本次压缩后要读多少字节。 客户端可以通过 If-Range 来区分资源文件是否变动，它的值来自 ETag 或者 Last-Modifled。如果资源文件有改动，服务器会返回code 200重新走下载流程。 为什么多线程下载可以提速见这篇文章：撸了个多线程断点续传下载器，我从中学习到了这些知识个人理解，当发生了丢包，检测到拥塞时，TCP慢启动会减半线性增长发送窗口。而多线程一起下载，其他线程可能正在正常下载，只影响发生丢包的线程 请求队列的使用的线程池Dispatcher类里有两个异步队列，一个同步队列，readyAsyncCalls是没排上队的请求。 没排上队是因为在进队时，目前正在执行的请求大于64或者同Host的请求大于5了，参考线程池的思想。runningSyncCalls和runningAsyncCalls都是正在执行（包括已经取消但没结束）的请求，区别是一个同步一个异步。结束后。在每个请求停止后会去promoteCall()，遍历readyAsyncCalls能够执行执行，放进runningAsyncCalls里。和线程池里的Worker一个套路。 okhttp责任链，递归调用拦截器根据网络的分层思想，把每一步要做的操作顺序排列为一个list。每个拦截器(Intercept)都要手动调用chain.proceed()去调用list里下一个index的拦截器(Intercept)来达到调用下一层的效果，并递归返回response。 除了发起真正的网络请求的CallServerInterceptor，它不用调用下一层，只用递归返回response 应用拦截器interceptors和网络拦截器networkInterceptors的区别在哪区别在应用拦截器是在整个操作前，网络拦截器是在真正请求前由于有多次重试等拦截器的存在，一般应用拦截器只会执行一次，网络拦截器可能会走很多次1234567891011121314151617181920212223242526Response getResponseWithInterceptorChain() throws IOException &#123; // Build a full stack of interceptors.interceptors List&lt;Interceptor&gt; interceptors = new ArrayList&lt;&gt;(); interceptors.addAll(client.interceptors());//应用拦截器 //重定向、重试 interceptors.add(retryAndFollowUpInterceptor); //用户应用层和网络从桥梁。主要包含： //1. 将用户的request，转变为网络层的request，比如添加各种请求头，UA ,Cookie , Content-Type等。 //2. 将网络层的response转变为用户层的response，比如解压缩，除去各种请求头等。 interceptors.add(new BridgeInterceptor(client.cookieJar())); //缓存 interceptors.add(new CacheInterceptor(client.internalCache())); //负责与服务器之间建立连接。 interceptors.add(new ConnectInterceptor(client)); if (!forWebSocket) &#123; interceptors.addAll(client.networkInterceptors());//网络拦截器 &#125; //负责请求服务器 interceptors.add(new CallServerInterceptor(forWebSocket)); //第一个chain Interceptor.Chain chain = new RealInterceptorChain( interceptors, null, null, null, 0, originalRequest); //通过链式请求的得到response return chain.proceed(originalRequest); &#125; OKHTTP Socket连接池复用Http连接减少请求延迟。避免网络连接的时延，以及避免TCP调谐带来的带宽过小的问题。okhttp记录了每个socket流使用情况，同时设定了每个socket能同时使用多少流。 socket复用有何标准 共享socket,HTTP/2支持所有连接到同一个主机的请求共享socket 对每个socket来说： host完全一样，可以直接复用return true 没有达到socket连接池复用数量的总上陷，且这个socket流还没有关闭 okhttp只在2.0的时候会复用 有相同IP地址的（说明两个对比的连接都已经DNS得到过IP），且没有使用代理，有了代理就不知道最初的IP是多少了 复用socket连接意味着跳过了https握手，所以要验证证书，如果证书可以覆盖的话，IP不一样也可以 一个socket如果被复用，只有http2.0允许同一个socket在同一个时候写入多个流数据。http1.x协议下当前socket没有其他流正在读写时可以复用，http2.0对流数量没有限制 http1.x在http 1.x协议下，所有的请求的都是顺序的，即使使用了管道技术（可以同时按顺序连续发送请求，但消息的返回还是按照请求发送的顺序返回）也是如此，因此一个socket在任何时刻只能有一个流在写入，这意味着正在写入数据的socket无法被另一个请求复用 http2.0http2.0协议使用了多路复用技术，允许同一个socket在同一个时候写入多个流数据，每个流有id，会进行组装，因此，这个时候正在写入数据的socket是可以被复用的。 一个socket何时会被关闭？socket池何时被清理okhttp通过一个单独的线程来清理socket池。每次put一个新连接的时候都会判断当前是否有在清理socket池。这个线程while(true)循环找出空闲时间最长的sockect连接，默认可以保存5个空闲线程，并且5分钟内依旧空闲的话，将被关闭回收。 规定时间t = sockect默认设定的生存时间 如果哪个空闲连接超过了规定时间t，关掉这个socket，立刻返回继续找下一个 如果每个空闲连接都没有超过规定时间t的，就返回空闲时间最长的sockect连接，得到diff = 规定时间 - 空闲时间，开始wait(diff)，wait()完后继续下一次清理 如果没有空闲连接， 最少wait()等待一个规定时间t 没有任何连接，返回-1退出while(true)，等待下次put个新连接的时候的时候重新开启清理socket池的线程只要连接池中有连接，基本清理线程就一直存在，直到所有连接被释放该线程才会停止 清理的线程池okHttp使用的是：123private static final Executor executor = new ThreadPoolExecutor(0 /* corePoolSize */, Integer.MAX_VALUE /* maximumPoolSize */, 60L /* keepAliveTime */, TimeUnit.SECONDS, new SynchronousQueue&lt;Runnable&gt;(), Util.threadFactory("OkHttp ConnectionPool", true)); 和newCachedThreadPool一样，可以动态添加到无限大的，用的SynchronousQueue当等待队列所以新来的线程没有地方去就会阻塞住，直到开了一个空线程来执行。Okhttp3 总结研究 （面试） OkHttp的HTTP缓存主要是根据Header(Date\Expires\Last-Modified\ETag\Age)来缓存响应数据减少重复的网络请求 点击桌面图标进入我们软件应用时发生了什么？通过翻阅 Application 启动的源码，当我们点击桌面图标进入我们软件应用的时候，会由 AMS 通过 Socket 给 Zygote 发送一个 fork 子进程的消息，当 Zygote fork 子进程完成之后会通过反射启动 ActivityThread##main 函数，最后又由 AMS 通过 aidl 告诉 ActivityThread##H 来反射启动创建Application 实例，并且依次执行 attachBaseContext 、onCreate 生命周期，由此可见我们不能在这 2 个生命周期里做主线程耗时操作。 OOM是什么，怎么导致的？ java.lang.OutOfMemoryError: Java heap space ——&gt;java堆内存溢出，此种情况最常见，一般由于内存泄露或者堆的大小设置不当引起。对于内存泄露，需要通过内存监控软件查找程序中的泄露代码，而堆大小可以通过虚拟机参数-Xms,-Xmx等修改。 java.lang.OutOfMemoryError: PermGen space ——&gt;java永久代溢出，即方法区溢出了，一般出现于大量Class或者jsp页面，或者采用cglib等反射机制的情况，因为上述情况会产生大量的Class信息存储于方法区。此种情况可以通过更改方法区的大小来解决，使用类似-XX:PermSize=64m -XX:MaxPermSize=256m的形式修改。另外，过多的常量尤其是字符串也会导致方法区溢出。 java.lang.StackOverflowError ——&gt; 不会抛OOM error，但也是比较常见的Java内存溢出。JAVA虚拟机栈溢出，一般是由于程序中存在死循环或者深度递归调用造成的，栈大小设置太小也会出现此种溢出。可以通过虚拟机参数-Xss来设置栈的大小。 事件分发 Android事件分发机制 详解攻略，您值得拥有 如何减少卡顿？刷新原理 Glide是如何监听周期的？监听生命周期用了一个没有UI的Fragment（名为RequestManagerFragment）入到传入的非Application的Context的FragmentManager中 监听传入的Context的AcitivityLifeCycle（由RequestManager实现的事实上） 监听网络状态依然是用上面的fragment的AcitivityLifeCycle回调达到的 在onStart和onStop的时候分别注册、反注册网络状态的监听，来达到在变得有网的情况下重启请求的目的 监听内存状态在Glide构造的时候会调用registerComponentCallbacks进行全局注册 registerComponentCallbacks是ApplicationContext提供具有内存过低等时机的回调（onTrimMemory、onLowMemory、onConfigurationChanged） 在onTrimMemory时，会依次执行： 123memoryCache.trimMemory(level);bitmapPool.trimMemory(level);arrayPool.trimMemory(level); 两个Activity切换生命周期是怎样的？ 两个Activity切换生命周期是怎样的？假设 从 A Activity 跳转到 B Activity lanchMode不同： B Activity 的 launchMode 为 standard ｜｜ B Activity 没有可复用的实例时：A.onPause -&gt; B.onCrete -&gt; B.onStart -&gt; B.onResume -&gt; A.onStop B Activity 的 launchMode 为 singleTop &amp;&amp; B Activity 已经在栈顶时：（一些特殊情况如通知栏点击、连点）此时只有 B 页面自己有生命周期变化: B.onPause -&gt; B.onNewIntent -&gt; B.onResume 当 B Activity 的 launchMode 为 singleInstance \ singleTask &amp;&amp; 对应的 B Activity 有可复用的实例时: A.onPause -&gt; B.onNewIntent -&gt; B.onRestart -&gt; B.onStart -&gt; B.onResume -&gt; A.onStop -&gt; ( 如果 A 被移出栈的话还有一个 A.onDestory) 摘自5 道刁钻的 Activity 生命周期面试题 生命周期回调都是 AMS 通过 Binder 通知应用进程调用的；而弹出 Dialog、Toast、PopupWindow 本质上都直接是通过 WindowManager.addView() 显示的（没有经过 AMS），所以不会对生命周期有任何影响。 如果是启动一个 Theme 为 Dialog 或 Transparent 的 Activity , 则生命周期为： A.onPause -&gt; B.onCrete -&gt; B.onStart -&gt; B.onResume。 注意这边没有前一个 Activity 不会回调 onStop，因为只有在 Activity 切到后台不可见才会回调 onStop； 弹出 Dialog 主题的 Activity 时前一个页面还是可见的，只是失去了焦点而已所以仅有 onPause 回调。 这是因为透明的弹窗前一个 Acitivity 被认为其实还可见 Activity 在 onResume 之后才显示的原因是什么 onCreate 方法里调用 setContentView 。里面是直接调用 window 的 setContentView，创建一个 DecorView 用来包住我们创建的布局。 加载好了布局，生成一个 ViewTree ，WindowManager#addView 方法最终将 DecorView 添加到 WMS。 在 onResume 回调之后，会创建一个 ViewRootImpl ，有了它之后应用端就可以和 WMS 进行双向调用了。同时也是通过 ViewRootImpl 从 WMS 申请 Surface 来绘制 ViewTree onActivityResult 在哪两个生命周期之间回调 You will receive this call immediately before onResume() when your activity is re-startingonActivityResult 回调先于该 Activity 的所有生命周期回调，从 B Activity 返回 A Activity 的生命周期调用为： B.onPause -&gt; A.onActivityResult -&gt; A.onRestart -&gt; A.onStart -&gt; A.onResume lateinit var和by lazylateinit var只是让编译期忽略对属性未初始化的检查，后续在哪里以及何时初始化还需要开发者自己决定。by lazy 用了双重检测初始化 SynchronizedLazyImpl， 保证变量在被取到的时候初始化一次 AsyncTask为什么坑？工程里用什么代替AsyncTask那？ 如果AsyncTask被声明为Activity的非静态的内部类，那么AsyncTask会保留一个对创建了AsyncTask的Activity的引用。 屏幕旋转或Activity在后台被系统杀掉等情况会导致Activity的重新创建，之前运行的AsyncTask会持有一个之前Activity的引用，这个引用已经无效。 doInBackground方法中有一个不可中断的操作，那么cancel掉是不会让AsyncTask停止的。cancel调用了thread的interrupt操作 在Android 1.6之前的版本，AsyncTask是串行的，在1.6至2.3的版本，改成了并行的。在2.3之后的版本又做了修改，可以支持并行和串行，当想要串行执行时，直接执行execute()方法，如果需要并行执行，则要执行executeOnExecutor(Executor)。 串行是通过一个ArrayDeque来完成的，这导致必须要任务必须要串行等待排队，如果中间谁阻塞时间过长会拖慢整个队列，而且不知道任务什么时候会执行即使用executeOnExecutor(AsyncTask.THREAD_POOL_EXECUTOR)通过线程池并行，这个线程池也是有最大并发数限制的，超过就会报错 AsyncTask的并行线程池是一个静态变量，所有 AsyncTask 共享一个线程池]]></content>
      <categories>
        <category>面试</category>
      </categories>
      <tags>
        <tag>面试</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[leetcode题目思考与记录]]></title>
    <url>%2F2020-02-29-leetcode%E9%A2%98%E7%9B%AE%E6%80%9D%E8%80%83%E4%B8%8E%E8%AE%B0%E5%BD%95.html</url>
    <content type="text"><![CDATA[移动0二叉树的中序遍历爬楼梯、括号生成 删除排序数组中的重复项盛水最多的容器跳表trim树 - 搜索自动补全TSP问题三重dp拓扑 + ？单调栈括号生成大根堆PriorityQueue默认是小跟堆 快排优化归并排序 剑指 Offer 51. 数组中的逆序对和 [315. 计算右侧小于当前元素的个数]都是归并排序的相同方法，区别在于左指针还是右指针入队时判断 归并排序优化 当递归到规模足够小时，利用插入排序（归并排序只是需要两个有序数组，小于7的时候使用插入排序可以更节省栈空间） 归并前判断一下是否还有必要归并（如果左边的最大的比右边的最小的还小(或者等于)，那就不用归并了，已经有序了） 只在排序前开辟一次空间（如文中所述， System.copyOfRange每次会分配空间，System.arraycopy 只是每次在已经开辟的空间上复制 ） 方法一 递归归并123456789101112131415161718192021222324252627282930313233343536373839404142434445public int reversePairs(int[] arr) &#123; int[] temp = new int[arr.length]; return sort(arr, 0, arr.length - 1, temp);&#125;// 变成 [left, mid] [mid + 1, right]的闭区间public int sort(int[] arr, int left, int right, int[] temp) &#123; if (left &gt;= right) return 0; // mid + 1的时候left可能会比right大1 int mid = (right - left) / 2 + left; // 防止溢出 int leftReverse = sort(arr, left, mid, temp); int rightReverse = sort(arr, mid + 1, right, temp); // 对已经排序后，两个有序数组来说，此处发现已经有序，不用继续 if (arr[mid] &lt;= arr[mid + 1]) return leftReverse + rightReverse; return mergeS(arr, left, mid, right, temp) + leftReverse + rightReverse;&#125;private int mergeS(int[] arr, int l, int m, int r, int[] temp) &#123; int total = r - l + 1; // 拷贝一遍当前排序状态的[l, r]当作辅助数组 // 一个用于指针遍历，一个用于存放结果 // 这里是修改了原数组存放结果，拷贝的辅助数组来遍历指针 System.arraycopy(arr, l, temp, l, total); int reverseNum = 0; int p = l; int q = m + 1; for (int i = l; i &lt;= r; i++) &#123; if (p &gt; m) &#123; // 左区间指针已经走完 arr[i] = temp[q++]; // 已经没有 &#125; else if (q &gt; r) &#123; // 右区间指针已经走完 arr[i] = temp[p++]; &#125; else if (temp[p] &lt;= temp[q]) &#123; // 辅助数组大于等于时，归并排序才是稳定排序（指相同数字依然按照原数组顺序排序） arr[i] = temp[p++]; // 左区间更小 &#125; else &#123; // 右区间更小 arr[i] = temp[q++]; // 右指针前进时，左区间尚存的数都是该数都逆序数（两数组各自有序，左区间尚存的数肯定比右指针之前一个指向的数大） reverseNum += m - p + 1; &#125; &#125; return reverseNum;&#125; 354. 俄罗斯套娃信封问题长宽都需要大于另一个信封时才能被套娃 按长升序，宽降序 长相同的信封不能套娃，所以长相同时需要按宽降序，忽略相同的长：使用LIS寻找不连续的上升序列的时候可以自动忽略相同的长 使用LIS对宽进行LIS，寻找不连续的上升序列 最长连续序列并查集，用v和v+1并组，每一个数字a的parent就是并查集里该组对应的最大数字b，b - a + 1 就是的距离取max ###12345678910111213141516public String[] largestNumberList(int[] nums) &#123; // Get input integers as strings. String[] asStrs = new String[nums.length]; for (int i = 0; i &lt; nums.length; i++) &#123; asStrs[i] = String.valueOf(nums[i]); &#125; // 把问题丢给string... Arrays.sort(asStrs, (a, b) -&gt; (b + a).compareTo(a + b)); // 降序 // 特别地，所有数都为0的情况下，返回0（因为前导0是要去掉的） if (asStrs[0].equals("0")) &#123; return "0"; &#125; return asStr;&#125; 46. 全排列 总个数是卡特兰数方法一，回溯，不重复元素下标，然后对应两个位置交换。这种不保证字典序12345678910111213public void recursion(List&lt;Integer&gt; a, int k) &#123; if (index == nums.length) &#123; res.add(new ArrayList&lt;&gt;(a)); return; &#125; for (int i = 0; i &lt; total; i++) &#123; for (int j = i; j &lt; total; j++) &#123; swap(a, i, j); // 注意重要的点在这里 recursion(a, k + 1); swap(a, j, i); &#125; &#125;&#125; 方法二，保证字典序，for循环每个位置选a里的数组，找出所有组合，然后用used去重12345678910111213141516171819public List&lt;List&lt;Integer&gt;&gt; permute(int[] nums) &#123; allComposeWithNoMap(0, nums, new ArrayList&lt;&gt;()); return resAll;&#125; private void allComposeNext(int index, int[] nums, List&lt;Integer&gt; res, Set&lt;Integer&gt; used) &#123; if (index == nums.length) &#123; resAll.add(new ArrayList&lt;&gt;(res)); return; &#125; for (int i = 0; i &lt; nums.length; i++) &#123; if (used.contains(nums[i])) continue; res.add(nums[i]); used.add(nums[i]); allComposeNext(index + 1, nums, res, used); res.remove(res.size() - 1); used.remove(nums[i]); &#125;&#125; 329. 矩阵中的最长递增路径 从某个位置出发的最大步数是固定的。 由于只用记步数，所以dfs四个方向最大的步数可以记忆化记下来 经过自己需要++1 memo记忆化数组不为空，表示搜索过以这个点出发 也可以用BFS+拓扑排序设(i,j)比相邻点大，出度为0，这就是边界条件计算全图出度，从所有出度为0的点开始BFS每遍历一个点，就拓扑排序去掉一个出度，若出度减为0，则入队BFS结果为BFS层序遍历的次数 螺旋矩阵可以构造visited[]数组记录去过的地方，然后用dir[]数组记录要走的方向，这个会比较通用一点的解法。简单解法是构造(l, t, r, b)方框，每走完一边就缩小范围，以下两个方法都是这样做的 方法一1234567891011121314151617181920212223242526// ---------// | |// | |// --------| private static List&lt;Integer&gt; calcBounds(int[][] matrix) &#123; List&lt;Integer&gt; res = new ArrayList&lt;&gt;(); int row = matrix.length; int col = matrix[0].length; int l = 0, t = 0, b = row - 1, r = col - 1; while (true) &#123; for (int i = l; i &lt;= r; i++) res.add(matrix[t][i]); if (++t &gt; b) break; for (int i = t; i &lt;= b; i++) res.add(matrix[i][r]); if (--r &lt; l) break; for (int i = r; i &gt;= l; i--) res.add(matrix[b][i]); if (--b &lt; t) break; // 此处t已经下挪过一了 for (int i = b; i &gt;= t; i--) res.add(matrix[i][l]); if (++l &gt; r) break; &#125; return res; &#125;&#125; 方法二1234567891011121314151617181920212223242526272829303132333435// ---------// | |// | |// |-------| private static List&lt;Integer&gt; calcBounds1(int[][] matrix) &#123; List&lt;Integer&gt; order = new ArrayList&lt;Integer&gt;(); if (matrix == null || matrix.length == 0 || matrix[0].length == 0) &#123; return order; &#125; int rows = matrix.length, columns = matrix[0].length; int l = 0, r = columns - 1, t = 0, b = rows - 1; while (l &lt;= r &amp;&amp; t &lt;= b) &#123; for (int column = l; column &lt;= r; column++) &#123; order.add(matrix[t][column]); &#125; for (int row = t + 1; row &lt;= b; row++) &#123; order.add(matrix[row][r]); &#125; if (l &lt; r &amp;&amp; t &lt; b) &#123; for (int column = r - 1; column &gt; l; column--) &#123; order.add(matrix[b][column]); &#125; for (int row = b; row &gt; t; row--) &#123; order.add(matrix[row][l]); &#125; &#125; // 统一缩小 l++; r--; t++; b--; &#125; return order; &#125; 约瑟夫环问题 见此 42. 接雨水 84. 柱状图中最大的矩形 保卫王国 - 牛客 矩阵最长递增路径同一个单元格对应的最长递增路径的长度是固定不变的。所以记忆化搜索从memo[i][j]开始的最长递增路径 无损压缩算法：deflate、Huffman Coding寻找两个有序数组的中位数要求算法的时间复杂度为 O(log(m + n))即找出第(m+n)/2个元素设k=(m+n)/2，则需要在两个数组中分别寻找第k/2个元素，原因如下： 题目中要求的时间复杂度为 O(log(m + n))，很容易想到的方法就是二分，现在有两个数组，要对那个数组进行二分合适？由于找的是中位数，那么这个数字的两边的元素个数是相等的，所以只需要确定一个数组中的两边元素，两一个数组的对应的补上去就可以了，为了提高效率，要选择最短的数组做二分查找 — 原文 为什么Java中int型数据取值范围是[-2^{31}, 2^{31}-1]为什么Java中int型数据取值范围是[-2^{31}, 2^{31}-1] 判断一个有符号int型整数是不是溢出：x &gt; INT_MAX / 10 || x == INT_MAX &amp;&amp; x 10 + 7 就会溢出x &lt; INT_MIN / 10 || x == INT_MIN. &amp;&amp; x 10 + 8 就会溢出 反转数字push一位数字x：int res = res * 10 + xPop一位数字：res /= 10需要警惕是否溢出 Lru最近访问Lru在Java里就是LinkedHashMap有序字典，每次get/put后把该元素挪动到双向链表尾。双向链表是因为方便删除，是所有数据的。、 2、3、5硬币面值（无限个数），构成总数为k的方法数和上楼梯很像，区别在于上楼梯只是1、2两步 上楼梯的状态方程： dp[0] = dp[1] = 1, dp[i] = dp[i-1] + dp[i-2] (i &gt; 2)。设置dp[0] = 1只是为了后续好加；这一题就是dp[1] = 1, dp[i] = dp[i-2] + dp[i-3] + dp[i-5]，意义是为了总数为i的金额是由i-2的情况和i-3和i-5的情况达成的 300. 最长上升子序列 LIS方法一 动态规划设dp[i]保存[0, i]的最长上升子序列 （可以不连续）中找到的一个比当前数i小的数j，如果第j个数字小于第i个数字，i就可以跟在j后面 dp[i] = Math.max(dp[i], dp[j] + 1)（0 &lt;= j &lt; i）1234567891011121314151617public int lengthOfLIS(int[] nums) &#123; int total = nums.length; int[] dp = new int[total]; // Arrays.fill(dp, 1); dp[0] = 1; //注意都要初始化为1，因为自己就是一个长度为1的序列 int max = 1; for (int i = 1; i &lt; total; i++) &#123; dp[i] = 1; for (int j = 0; j &lt; i; j++) &#123; if (nums[i] &gt; nums[j]) &#123; dp[i] = Math.max(dp[i], dp[j] + 1); &#125; &#125; max = Math.max(max, dp[i]); &#125; return max;&#125; 方法二 贪心 + 二分要使上升子序列尽可能的长，得让序列上升得尽可能慢，在上升子序列最后加上的那个数尽可能的小。基于上面的贪心思路，维护一个数组 d[i] ，录目前最长上升子序列的长度，遍历数组看是否能二分插入其中获得最小值最后数组的长度就是LIS1234567891011121314151617181920212223public int lengthOfLIS(int[] nums) &#123; int total = nums.length; int len = 0; // len = 下次要插入的地方 int[] a = new int[total + 1]; // minElements 保存所有最小的上升数字，如果更小的数字可以插入到这个数组里，则替换掉 for (int i = 0; i &lt; total; i++) &#123; // find where to insert in minElements 寻找在比它更小的数字后面插入 int l = 0, r = len - 1; while (l &lt;= r) &#123; int mid = (r + l) &gt;&gt; 1; if (a[mid] &lt; nums[i]) &#123; l = mid + 1; // 插入到这个数后 &#125; else &#123; r = mid - 1; &#125; &#125; // every element is larger than m[i], clear the array and set to m[0] 没找到比它更小的数字 a[l] = nums[i]; if (l == len) &#123; // 比所有位置都大，需要在后面新加一个 len++; &#125; &#125; return len;&#125;]]></content>
      <categories>
        <category>面试</category>
      </categories>
      <tags>
        <tag>面试</tag>
        <tag>Algorithm</tag>
        <tag>算法</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[数据库面试答案]]></title>
    <url>%2F2019-07-04-%E6%95%B0%E6%8D%AE%E5%BA%93%E9%9D%A2%E8%AF%95%E7%AD%94%E6%A1%88.html</url>
    <content type="text"><![CDATA[子查询的数据库效率差别比较 in和or区别在需要使用的这个栏位所在的列为索引或者主键时，使用in或者or的区别不是很大。在没有索引的情况下，随著in或者or的数量越来越多，in的效率不会有很大的下降，但是or会随著记录越来越多的情况下效能下降很快。 not exists 和 not in 比較時，not exists 的效率比較高 in是先执行子查询中的查询，然后再执行主查询，故适合于外表大而内表小的情况。in时不对NULL进行处理。 exists查询它是先执行主查询，即外层表的查询，然后再执行子查询，故适合于外表小而内表大的情况。 当使用in时，子查询where条件不受外层的影响，自动优化会转成exist语句，它的效率和exist一样。如select * from t1 where f1 in (select f1 from t2 where t2.fx=’x’) 这时，认为in 和 exists效率一样 一些常见的sql性能问题]]></content>
      <categories>
        <category>面试</category>
      </categories>
      <tags>
        <tag>面试</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[当你发现自己不知道做什么...]]></title>
    <url>%2F2019-01-28-%E5%BD%93%E4%BD%A0%E5%8F%91%E7%8E%B0%E8%87%AA%E5%B7%B1%E4%B8%8D%E7%9F%A5%E9%81%93%E5%81%9A%E4%BB%80%E4%B9%88.html</url>
    <content type="text"><![CDATA[去看面试笔记吧！有目标才能快速发现自己不会什么~ 2018年年底Android悲催的面试之路 what we need to know 关于Handler的拦截里有谈到，调用callback的时候是根据顺序选择一个来调用的。 优先调用Message中的Callback，再来调用Handler的全局Callback；如果没有全局mCallback，或者全局mCallback返回false不拦截，则调用Handler的handleMessage epoll_wait是什么唤醒机制？ 系统是如何监听ANR的? 应用发生卡顿，一定是在dispatchMessage中执行了耗时操作。通过给主线程的Looper设置一个Printer，计算方法运行前后两次打印的时间差是否超过阈值来判断是否有ANR。tips： /data/anr/traces.txt中会保存一个最近ANR的信息。 ThreadLocal所操作的变量只存在于各自的线程里，每一个线程都有一个ThreadLocal且拿到的变量是不同的。每个Thread的对象都有一个ThreadLocalMap，当创建一个ThreadLocal的时候，就会将该ThreadLocal对象添加到该Map中，其中键就是ThreadLocal，值可以是任意类型 《深入理解java虚拟机》周志明，P396原文：要保证线程安全，并不一定就是要进行同步，两者没有因果关系。同步只是保证共享数据争用时的正确性的手段。如果一个方法本来就不涉及共享数据，那它自然就无需任何同步措施去保证正确性 在华为的好多手机会OOM是由线程数溢出引起如何解决？java.lang.IllegalArgumentException: register too many Broadcast Receivers，华为5.0+的手机发送超过500个广播就会出现这个问题，因为它内部有白名单，只允许白名单中的应用（微信）发500个广播。这篇绕过华为register too many Broadcast Receivers限制里有用到一些插件化的知识，推荐阅读插件加载机制]]></content>
  </entry>
  <entry>
    <title><![CDATA[Android踩过的坑]]></title>
    <url>%2F2019-01-25-Android%E8%B8%A9%E8%BF%87%E7%9A%84%E5%9D%91.html</url>
    <content type="text"><![CDATA[错误使用PorterDuffXferMode后效果并没有得到想要的效果！-&gt; 关了硬件加速还是不行555 为何和谷歌给的样子不同？因为谷歌写的demo里，是两个高宽一样的bitmap在相交，如果发现有多余的位置没有被遮住，说明两个相交的位置不一样。 源图像在运算时，只是在源图像所在区域与对应区域的目标图像做运算。所以目标图像与源图像不相交的地方是不会参与运算的！这一点非常重要！不相交的地方不会参与运算，所以不相交的地方的图像也不会是脏数据，也不会被更新，所以不相交地方的图像也永远显示的是目标图像。 —— 《Android高级进阶——绘图篇（五）setXfermode 设置混合模式》 谁是源图像谁是目标图像123456//绘制 目标图像canvas.drawBitmap(dstBitmap, 100, 100, paint);//设置 模式 为 SRC_OUTpaint.setXfermode(new PorterDuffXfermode(PorterDuff.Mode.SRC_OUT));//绘制源图像canvas.drawBitmap(srcBitmap, 100, 100, paint); 任何和空白透明像素相交的结果都是空白透明像素哦所以SRC_OUT等需要有颜色，而不是透明的。比如SRC_OUT时需要添加颜色。 例子 用蒙版做了一个指针指示，里面有提到关于带透明度的混合模式与谷歌给的Api展示的区别：Android 混合模式 —— PorterDuffXferMode 基于谷歌Api的demo修改了让你测试：Canvas绘图之PorterDuffXfermode使用及工作原理详解 给了一个demo让你自己尝试不同的效果： 关于 Xfermode 正确理解姿势 做了一个DST_ATOP的例子：android遮罩Xfermode的学习 解释了SRC_OUT：通过消除背景层与重叠部分绘制组合图形 到底有没有在用心学ndk啊 JNIEnv*env 是一个线程对应一个env，线程间不可以共享同一个env变量。JNI中Fatal signal 11 (SIGSEGV), code 1的错误 可以使用Android自带的addr2line来分析native的行信息Android通过addr2line工具分析native crash log 注意lock后再wait该锁的问题JVM故障分析系列之五：常见的Thread Dump日志案例分析 使用SurfaceView时需要注意 All SurfaceView and SurfaceHolder.Callback methods will be called from the thread running the SurfaceView’s window (typically the main thread of the application). They thus need to correctly synchronize with any state that is also touched by the drawing thread. You must ensure that the drawing thread only touches the underlying Surface while it is valid – betweenSurfaceHolder.Callback.surfaceCreated() andSurfaceHolder.Callback.surfaceDestroyed(). 硬件加速？ Android里所有View控件以自定义View所使用得Canvas是硬件加速的（使用 GPU 进行加速，在 Android 上一般就是指会使用 OpenGL 进行绘制），Surface.lockCanvas获取的就是关闭硬件加速的Canvas。见OpenGL中 Canvas 性能分析 SurfaceView/TextureView的区别 SurfaceView是一个有自己Surface的View。界面渲染可以放在单独线程而不是主线程中。它更像是一个Window，自身不能做变形和动画。 TextureView同样也有自己的Surface。但是它只能在拥有硬件加速层层的Window中绘制，它更像是一个普通View，可以做变形和动画。 普通View都是共享一个Surface的，所有的绘制也都在UI线程中进行，因为UI线程还要处理其他逻辑，因此对View的更新速度和绘制帧率无法保证。这显然不适合相机实时 预览这种情况，因而SurfaceView持有一个单独的Surface，它负责管理这个Surface的格式、尺寸以及显示位置，它的Surface绘制也在单独的线程中进行，因而拥有更高 的绘制效率和帧率。 非UI线程使用View.post，可能会导致内存泄漏 在API低于24的版本上，非UI线程使用View.post，可能会导致内存泄漏。因为子线程调用View.post()，如果view还未attach到window，只有UI线程的performTraversals才会去把runnable拿出来执行，子线程没有performTraversals，故这个runnable将永远不会执行。 文章中最后给出的结论是，要保持兼容 API 24 以下的版本，最好不要在非 UI 线程使用 View.post ，而采用 Handler 来 post runnable。 当然在 API 24 及以上，如果不能保证 View 一定会被 attach，那可以在引用对象销毁时，使用 View.removeCallbacks。 Fragment的坑 子fragment中写了listener，父fragment设置子fragment的listener，然后横竖屏切换时，listener会报空： 在activity被异常回收之后，如果没有设置onConfigurationChange，activity会重建完整再走一遍生命周期的流程。onSaveInstanceState方法被调用的时候，已经add到FragmentManager中的fragment不被释放，旧的fragment的内存实例始终存在，且view也始终在展示并不会被destroy，因此需要在Acitivty的onCreate()和onSaveInstanceState()处添加代码：123if (savedInstanceState != null) &#123; savedInstanceState.remove("android:support:fragments");&#125; Android Studio 的新东西 find . | grep hprof-conv 在~\Library\Android\sdk\下找出hprof-conv的位置，再使用命令./hprof-conv -z infile outfile 来转换成Eclipse的Mat工具能够识别的hprof文件，注意使用此命令时，指定的outPutFile路径需要已经被创建，否则会一直返回Usage…展示Usage: hprof-conf [-z] infile outfile 强行刷新gradle依赖缓存，拉取远程依赖版本：./gradlew build --refresh-dependencie。 只想刷新某个指定的依赖: 直接去~/.gradle/caches/modules-2目录下，rm -fr find . -name xxx，然后直接重编删除含有’aaa’的文件夹依赖：find ~/.gradle/caches -type d | grep &#39;aaa&#39; | xargs rm -r CoordinaryLayout-Behavior达成滑动联动特效 Android常规的Touch事件传递机制是自顶向下，由外向内的，一旦确定了事件消费者View，随后的事件都将传递到该View。因为是自顶向下，父控件可以随时拦截事件，下拉刷新、拖拽排序、折叠等交互效果都可以通过这套机制完成。Touch事件传递机制是Android开发必须掌握的基本内容。但是这套机制存在一个缺陷：子View无法通知父View处理事件。NestedScrolling就是为这个场景设计的。 ———— Android Nested Scrolling 在Window上做动画：Acitivity间自定义动画的可能性这两天一直在想做一个Activity间的转场动画可以对单个view进行自定义动画的。因为ShareElement动画不支持5.0以下，所以开始思考其他的方法。读了通过WindowManager添加view以及添加动画后发现其实shareElemet也是在做假动画，本activity一次假动画，下一个Activit一次假动画。这篇Android WindowManager及其动画问题中说到，在WindowManager上像在Activity中使用动画效果无效，目前还没细看。 发现了一个特殊的效果..图凌闪屏页及Android彩蛋探究目前的结论还是做假的动画比较靠谱…]]></content>
      <categories>
        <category>Android</category>
      </categories>
      <tags>
        <tag>Android</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[算法简明笔记]]></title>
    <url>%2F2019-01-23-%E7%AE%97%E6%B3%95%E7%AE%80%E6%98%8E%E7%AC%94%E8%AE%B0.html</url>
    <content type="text"><![CDATA[简略地描述思路，以供后来观之 本笔记只是备忘录，并不是详细解决方案，请使用ctrl+F查询 Algorithm1. 打印字符串全排列递归后需要马上还原（因为所有的），因为递归是一颗深度优先的树（也就是栈），是先从叶子开始改变的 2. 二叉搜索树转为双向链中序遍历二叉搜索树正好是递增的，只需要把结点左右指针连接中序中前后的结点。（二叉搜索树没有相同值的点） 3. 递归求排列求排列时若用递归法，递归叶子回溯到父亲时需要还原改变（叶子最先改变，也就是说是从字符串数组最后两位开始的），不然就不是同一串字符串出发的（画出排列树推吧） 3. 复制复杂链表每个结点复制一个接在该结点后面，a-&gt;a’-&gt;b-&gt;b’-&gt;c-&gt;c’，同时复制原结点所指向的任意结点，最后奇偶结点分离（跳着分离） 4. 最小的k个数 partition函数（快排）最后得到的index就是他在序列中的位置，左边的就是前index个数。（期间可以用二分法逐渐缩小区间，由于是用一个固定位置的数作pivot所以最后得到的index是针对整个数组而言的） 5. 集合和最大堆基于红黑树6. 最大子数组和如果累加和为负则重新从新元素开始 7. 从1到n数里包含1的个数O(log10N) ， 十位上，每轮出现10次是指[10,19]这10个数 解析 8. 逆序对归并排序的同时进行计数 9. 数组排成最小的数可以用sprintf将数字写入字符串 10. 二叉树最低公共祖先（若是二叉搜索树）比一个结点大，比另一个结点小；（若有指向父节点的指针）寻找两个链表的公共结点；（什么都不是）用栈递归到叶子再逐个往上退栈。 11. 二叉树路径和等于某个数传递vector的引用（用vector作栈方便顺序输出），递归遍历，进入结点时压入该结点，退出时弹出（恢复成父节点进入的情景） 12. 不能继承的类虚拟继承virtual 父类，把父类构造函数设为私有，并且父类含有一个友元函数，友元函数的类型是由模板类给出的，所以虚拟继承的时候需要把子类的类型当成参数传入；（只能在堆上）私有构造与析构，用静态getInstance函数返回一个新的父类对象。 13. C++按类中声明的元素顺序初始化成员14. 求方程正整数解个数求方程正整数解个数 15. 多路平衡归并排序外排序，m个归并项（子表个数）k路归并的归并趟数s=logk(m)，败者树 16. n阶乘末尾0的个数（n除以5，即求5的质因数个数）存在0必然是52的结果，所以必然含有5!(5432*1)，所以只要求多少个5的倍数即可 17. mongodb为什么没有自增id因为并发情况下同时插入,分布式存储的情况下无法协调分配连续数字（协调需要消耗网络资源），并且内部也不是按id顺序存的，因为文档长度可变，不知道什么时候文档变长要放到后面去 18. 数n的约数个数（约数：能把别人整除的叫约数）自然数的约数的个数是有限的，质数的约数是1和本身；合数一定有3个以上的约数。由于约数个数定理知道：约数的个数等于:所有质因数的指数加上1后的乘积;若一个数分解质因数后为(a^m)*(b^n),其中a,b均为质因数;m,n均为相应质因数的指数.则约数个数为(m+1)(n+1).(因为a^0,a^1 … a^m，都是它的约数共m+1个) 例如:(1)12=2^2 3,质因数有2和3,其指数分别为2和1,那么12的约数有(2+1) (1+1)=6(个);(2)60=22 3 5,质因数2, 3, 5的指数分别为2, 1, 1,那么60的约数有(2+1) (1+1) (1+1)=12(个) 19. 一个数所有约数之和一个数所有约数之和 等于先把每个质因数从0次幂一直加到其最高次幂,再把每个相应质因数幂的和相乘.若一个数分解为(a^m)*(b^n),则这个数所有约数的和为:(a^0+a^1+a^2+a^3+…+a^m)(b^0+b^1+b^2+b^3+…+b^n). 例如:(1)12=223,则12所有约数的和为：(2^0+2^1+2^2)(3^0+3^1)=74=28;(2)60=2235=(2^0+2^1+2^2)(3^0+3^1)(5^0+5^1)=74*6=168. 20. Trie 树21. 需要同时移动多少步A和B才同时指向一个节点长度为100的循环链表，指针A和指针B都指向了链表中的同一个节点，A以步长为1向前移动，B以步长为3向前移动，一共需要同时移动多少步A和B才能再次指向同一个节点？ 假定经过n步A、B再次相遇。则A经过的结点为n，B经过的结点为3n；此刻B必然比A多经过了整数倍的链表长度（圈的长度），假定经过了i倍的链表长度，则有3n-n=100i，即2n=100i；满足该等式的最小整数位i=2，n=100。即A经过了100个结点，B经过了300个结点，二者再次相遇。 也可以认为跑圈100m，A速度1m/s B速度3m/s，B比A多跑一圈耗时100/（3-1）=50s 22. 模拟加减乘注意 ^按位异或 、&amp; 按位与 、| 按位或 加法运算将一个整数用二进制表示，其加法运算就是：相异（^）时，本位为1，进位为0；同为1时本位为0，进位为1；同为0时，本位进位均为0。 故不计进位的和为sum = a^b，进位就是arr = a&amp;b,(与sum相加时先左移一位，因为这是进位）。完成加法直到进位为0.123int Add(int a, int b)&#123; return b ? Add(a^b, (a&amp;b)&lt;&lt;1) : a;&#125; 求相反数1234//求a的相反数：将各位取反加一int negative(int a)&#123; //get -a return Add(~a, 1);&#125; 减法运算a-b = a+(-b) 根据补码的特性，各位取反加1即可（注意得到的是相反数，不是该数的补码，因为符号位改变了）（上面用二进制实现的加减法可以直接应用于负数）1234int Minus(int a, int b)&#123; return Add(a, negative(b));&#125; 乘法运算原理上还是通过加法计算。将b个a相加参考二进制数转十进制数，每位有一个2次幂的权重，当该位为1时，加上 2^x 倍 a所以权重 a 不断 a&lt;&lt;=112345678910111213141516171819202122//正数乘法int Multi(int a, int b)&#123; int ans = 0; while(b)&#123; if(b&amp;1) ans = Add(ans, a); a = a &lt;&lt; 1; b = b &gt;&gt; 1; &#125; return ans;&#125;//正数除法int Divide(int a, int b)&#123; int coun = 0; while(a &gt;= b) &#123; a = Minus(a, b); coun = Add(coun, 1); &#125; return coun;&#125; 除法运算除法运算是乘法的逆。看a最多能减去多少个b， 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051//判断是否是负数，0，正数int isneg(int a)&#123; return a &amp; 0x8000;&#125;int iszero(int a)&#123; return !(a &amp; 0xFFFF);&#125;int ispos(int a)&#123; return (a&amp;0xFFFF) &amp;&amp; !(a&amp;0x8000);&#125;//处理负数的乘法和除法int My_Multi(int a, int b)&#123; if(iszero(a) || iszero(b)) return 0; if(isneg(a)) &#123; if(isneg(b)) return Multi(negative(a), negative(b)); else return negative(Multi(negative(a), b)); &#125;else if(isneg(b)) return negative(Multi(a, negative(b))); else return Multi(a, b);&#125;int My_Divide(int a, int b)&#123; if(iszero(b)) &#123; cout &lt;&lt; "Error!" &lt;&lt; endl; exit(1); &#125; if(iszero(a)) return 0; if(isneg(a)) &#123; if(isneg(b)) return Divide(negative(a), negative(b)); else return negative(Divide(negative(a), b)); &#125;else if(isneg(b)) return negative(Divide(a, negative(b))); else return Divide(a, b);&#125; 计算统中数值一律用补码来表示,因为补码可以使符号位和数值位统一处理，同时可以使减法按照加法来处理。原码 -&gt; 补码： 数值位取反加1 数值位不包括符号位补码 -&gt; 原码： 对该补码的数值位继续 取反加1补码的绝对值：（称为真值）正数的真值就是本身，负数的真值是各位（包括符号位）取反加1（即变成原码并把符号位取反） b -&gt; -b ： 各位（包括符号位）取反加1 旋转字符：字符串前面的部分排在后面“abcd”的旋转串是”abcd”，”bcda” … “dabc”首先目标串与原串长度要相等 可以设字符s+s，只要是s+s的子串都是旋转字符比如：&quot;abcd&quot;+&quot;abcd&quot;，即&quot;abcdabcd&quot;中寻找子串，时间复杂度是根据查找子串的时间来定的，所以如果是采用KMP则是O(N) 字符串提取出现的正负整数总和注意可以负负得正设置符号位positive，cur当前字符，num每一位不断往高位挪的积累数（字符串转化为数字），res数字总和 当cur为数字时，num = num * 10 + positive? cur:-cur; 当cur不为数字的时候，说明此时刚刚结束数字的转化，或者是开始部分没有数字，无论哪种都应该加上刚刚转化的数字num，并把num置零重新开始计数此时需要注意当cur为负号时，前一位为负则positive=true，否则为负；如果不是负号，则重置positive为正 字符串转换成int范围内数字字符串可以是只出现一个0，即 0字符串不能出现 - 、-0 、-02 、A12如果没有出现违法，只要检查是否每个字符都是数字就可以， 接下来进入判断，由于int范围是-2147483648 到 2147483647先把所有的数字都转为负数（转为负数可以不用再写一遍正数的，最后如果是整数再负负得正回去） 判断是否溢出，因为-214748364 * 10 + (-8) 刚好是最大整数先判断是否 ( res &lt; = -214748364 )|| (res == -214748364 &amp;&amp; cur &lt;= -8 )同时也要注意如果是正整数，则转换后不能大于 2147483647字符串replace a with b12345678910for(for...)&#123; if(a[i++]==b[j])&#123; j++ ; if(j==源串长)&#123; // 不用源串长-1，前面++过了 标记a串 [i-源串长,i] 为0或者某个特定符表示需要删掉 //（为了节省时间我们可以不删掉，只是不打印就可以） &#125; &#125; else j=0;&#125; 当a[i]=0而a[i-1]!=0时表示可以开始将前面积累的字符串打印了res = res + cur + to注意最后是如果末尾全是0，没有到不是0的过度，我们需要再手动把这部分转换加上 res += cur 计算连续出现字符的个数aabbbcc -&gt; a_2_b_3_c2这种连续计数，在字符变化后结束上一轮计数的都是一种做法，注意末尾部分没有转换需要手动加上 本题从第二个字符开始判断每个字符和前一个字符如果不一样，进入转换，记录数字个数，重置计数个数为1（因为当转换出现，和前个不一样，前个计数为1，例如ab，从b开始，a计数为1） 如果是 a_2_b_3_c2 -&gt; aabbbcc，求第5个字符是什么，此处是b由于有”_”，可以因此分别数字与字符，保存当前字符cur，记录数字，当数字大于需要求的位置则返回 判断是否每个字符只出现一次可以用Map但是如果只花费O(1)空间，只能使用堆排序，将数组排序对比前后是否相同 浮点数高精度幂替换空为%20先扫描一遍记录总长度为len+2*blank，再从后往前复制 一般挪动数组元素的，都需要从右往左复制 reverse字符串reverse整个串while(start&lt;end){ swap(c[start],c[end]); start++; end–;} reverse前后部分顺序abcde-&gt;deabc先翻转abc，再翻转de，再翻转整个串 变成回文串需要的最少字符str长为N，$N*N$ 的动态规划表 dp[i][j]表示str[i..j]这段字符最少需要多个字符使str[i..j]变成回文串 dp[i][j]=$$\begin{cases} 0, &amp; i=j\ 0 , &amp; str[i]=str[j] \quad and \quad |i-j|=2\ 1 , &amp; str[i]!=str[j] \quad and \quad|i-j|\geq2\ dp[i+1]dp[j-1] , &amp; str[i]=str[j] \quad and \quad |i-j|\geq2\ max{dp[i+1][j],dp[i][j-1]}+1 , &amp; str[i]==str[j] \quad and \quad |i-j|\geq2&amp;\end{cases}$$ 未完待续 判断最长左右括号()() 与 (())合法，返回最长的合法出现的括号位置 先检查( 和 )的数量是否相等 设dp[i]表示str[0..i]位置上，以str[i]结尾的最长有效括号长度 $….(()())为6$ str[i]==(，有效括号长度子串以)结尾，故dp[i]=0; str[i]==)，dp[i-1]代表str[i-1]结尾的最长有效括号长度，上一处没有被匹配过的位置i-1-dp[i-1]上如果是(，可以和str[i]凑成一对，此时dp[i]=dp[i-1]+2，同时dp[i-1]前面即str[i-2]之前的dp[i-2]也应该算上 最后求解 Max(dp[0..N-1]) 就是最终结果 计算字符串 4((3(-4)+-4*3))的结果每次碰到(就进入递归进入下一层，)就结束递归，并传递参数 (char [], 下一个位置位置 )数组返回(计算的结果, 结束的位置)1234567891011121314151617Recur(char [] , int i)&#123; while(i &lt; length &amp;&amp; ch[i] !=')')&#123; if( ch[i] 是数字 )&#123; pre = pre * 10 + ch[i] - '0'; i++; &#125;else if (ch[i] 是操作符 )&#123; 队列a入队pre 队列a入队ch[i] pre = 0; i++; &#125;else(ch[i] == '(')&#123; array = 递归本函数(ch,i+1) pre = array[0]; i = array[1]+1; &#125; &#125;&#125; 整数N的二进制全排中0左边必有1的个数按顺序拼接字符串使总结果字典序最小 先按照拼接的方式将数组排列，即 return (a+b)&gt;(b+a) 将排序后数组从头到尾组合起来 贪心算法，有详细需证明它的贪心策略是正确 新类型字符a、Aa、AA 中第k个字符是哪种类型的从k-1个字符开始往左对大写字母计数，碰到小写字母停止 根据奇偶判断k和k+1是AA，还是Aa，还是k+1是单独的a 回文段最少切割数dp[i]是子串str[i..len-1]需要切几次才能使str[i..len-1]全切成回文串，dp[0]是最后结果 从右往左，i初始为len-1 如果 str[i..j]是回文串,dp[i]=dp[j+1]+1，只需从str[j+1..len-1]寻找最经济的切割法 j 从 [i..len-1] 遍历，dp[i]=Min{dp[j+1]+1},i&lt;=j&lt;len且str[i..j]为回文串 快速判断是否是回文串的方法是回文子串长度的相同方法，设p[i][j]为i..j间是否是回文串的判定以下情况p[i][j]是回文串： str[i..j]长度为1 str[i..j]长度为2，且2个字符相等 子串str[i+1..j-1]是回文串（即p[i][j]=true），且str[i]==str[j]，最外面的两个字符串相等。 i 是从右往左，j是从i往右，所以i..j间长度是展开的便于计算p[i][j] 推荐阅读： 【阿里】算法工程师笔试题整理（13&amp;14年） 剑指offer题目及答案]]></content>
      <categories>
        <category>算法</category>
      </categories>
      <tags>
        <tag>Algorithm</tag>
        <tag>算法</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Android 警醒向条例]]></title>
    <url>%2F2019-01-23-Android%E4%BB%A3%E7%A0%81%E8%A7%84%E8%8C%83%E6%9D%A1%E4%BE%8B.html</url>
    <content type="text"><![CDATA[依然很简洁的条例，备忘用 本篇是搜集关于Android的仅以提醒作用的条例，见名知意。 相关注意事项推荐阅读《阿里巴巴的安卓手册》 条例 onActivityResult()发生在onResume()之前 onFinishInflate()在加载完成xml后执行：只有当自定义View，通过在XML中创建时才会调用。不从xml布局文件中解析的话，比如通过new方式创建，则不会执行该方法； foreach使用前需要判空 overridependingtransition()进入动画需要再startActivity之后，退出动画需要再finish后 view的调用过程:构造方法-&gt;onFinishInflate-&gt;onSizeChanged-&gt;onDraw 如果用户finish()方法结束了Activity，则不会调用onSaveInstanceState() getIntrinsicWidth()和getIntrinsicHeight() canvas的好处之一是旋转画布后旋转回来，以达到图像绘制部分的旋转，不用对坐标点进行转置 用transient关键字标记的成员变量不参与序列化过程 滑动冲突处理：可以根据滑动距离和水平方向形成的夹角；或者根绝水平和竖直方向滑动的距离差；或者两个方向上的速度差等 getParent().requestDisallowInterceptTouchEvent(false)请求父亲不要阻挡儿子 getSuggestedMinimumWidth的逻辑：View如果没有背景，那么返回android:minWidth这个属性指定的值，这个值可以为0；如果设置了背景，则返回背景的最小宽度和minWidth中的较大值。 Fragment的动画setCustomAnimations()对于反射取FragmentManager中置儿子为null的是有问题的，建议用Fragment自己的onCreateAnimation()函数 OOM：尽量避免使用帧动画，使用的话应尽量避免使用过多尺寸较大的图片 内存泄露：属性动画中的无限循环动画需要在Activity退出的时候及时停止，否则将导致Activity无法释放而造成内存泄露。view动画不存在这个问题； 当调用到onNewIntent(intent)的时候，需要在onNewIntent() 中使用setIntent(intent)赋值给Activity的Intent.否则，后续的getIntent()都是得到老的Intent launchMode为singleTask的时候，通过Intent启到一个Activity,如果系统已经存在一个实例，系统就会将请求发送到这个实例上，但这个时候，系统就不会再调用通常情况下处理请求数据的onCreate方法，而是调用onNewIntent方法 DiffUtil.DiffResult可以使RecyclerView不添加相同的条目，即部分更新 RelativeLayout如果cast到FrameLayout是不必要的，view就可以 自定义view，一定是this(context)、this(context,attr)、super(context, attr, defaltStyle)，注意需要手动调init()或者this(context, null)与this(context,attr,0) 小心使用ArrayList的subList [a, b) ，对生成对新列表的改变会改变原列表结构，可以在新列表前加final来阻止后续更改 RequestLayout() , Invalidate() , layout()的区别 如果需要addView的话，那么使用getLayoutParams是没用的，因为这个你自己新建的一个View，没有与父容器有任何关联。所以LayoutParams也需要你自己创建 contentInsetStart消除ToolBar在自定义布局中的左部白边 JSONObject中的getString()和optString()方法的区别在于optString()不报错 @Transient标志则不入数据库 overridePendingTransition转移 由于Activity的TaskMode不同所以onActivityResult在Activity的生命周期中执行的先后也是不同的，更有onCreate不会执行的情况，所以onActivityResult有时候提前结束收不到result。 调用startActivityForResult后onActivityResult立刻响应，而返回当前页时onActivityResult不响应主要是launchMode的问题： 第一种：当resultCode==0时，Activity的launchMode必须设置为”singleTask”或者singleInstance，否则就会出错。这是因为从Task的角度看，Android认为不同Task之间的Activity是不能传递数据的，所以不能使用NEW_TASK标识，但还是要调用forResult方法。 第二种：当resultCode！=0时，Activity的launchMode必须为standard（删除launchMode属性默认为standard模式），原因同上。 只有第一层fragment会收到onActivityResult 重写onBackPressed() 自定义回退事件可以避免按Back键自动设置resultCode为RESULT_CANCELED的问题 requestCode &gt;=0就好，随便用于在onActivityResult()区别哪个子模块回传的数据，每个区分开不同的requestCode resultCode 如果B子模块可能有几种不同的结果返回，可以用setResut(int resultCode, Intent intent)予以识别区分 @IntDef 注解自定义了一个 OrientationType 注解，用于防治用户随意设置数值。使用 @IntRange 方法将行列数限制在一个较合理的范围内。 1234@IntDef(&#123;VERTICAL, HORIZONTAL&#125;)public @interface OrientationType &#123;&#125; // 滚动类型@OrientationTypeprivate int mOrientation = HORIZONTAL; // 默认水平滚动 View child = recycler.getViewForPosition(i); //如果不存在的话 RecyclerView 会自动创建 RecyclerView要先移除再添加，这样会移除的 View 会被先放到缓冲区中，再添加 View 时就可以直接从缓冲区中把被移除的条目直接取出来使用了，而不用重新创建，以减少开销；如果先添加，由于缓冲区中没有可以使用的 View，会进行先创建，之后再添加到界面上，最后执行移除操作会导致有大量的 View 滞留在缓冲区中，会造成严重的性能浪费 对Vector、ArrayList在迭代的时候如果同时对其进行修改就会抛出 java.util.ConcurrentModificationException 异常 LinkedList在遍历循环中get(i)时间复杂度为O(n)，比ArrayList的O(1)性能差，使用有迭代器(Iterator)以及ForEach循环来对LinkedList进行遍历 对Activity来说，所有需要传递或接收的Activity不允许设置launchmode=”SingleTask”，只能设为标准模式，否则系统将在 startActivityForResult() 后直接调用 onActivityResult() Builder模式下，需要Builder单独列出的原因之一是，在使用单例模式时，每次都是用的同一个instance，而参数每次都需要重新new一个；不单独写一个Builder出来（第二重）是由于每次都重新new了一个XXX实例，当然参数也每次都不一样 12345678910111213141516171819202122232425//第一种class XXX&#123; //普通单例模式 private static XXX instance; public static XXX getInstance()&#123; if(instance == null)&#123; return new instance; &#125; &#125; private XXX&#123;&#125; class Builder()&#123; void set()&#123; ... return Builder.this; &#125; &#125;&#125;//第二种class XXX&#123; void set()&#123; ... return XXX.this; &#125;&#125; 基于引用计数的垃圾回收器无法处理循环引用导致的内存泄露问题，但是其在主流的JVM中很少，几乎所有的JVM都是采用引用对象遍历的方法，垃圾回收器都会处理循环引用潜在的问题 ThreadLocal、Volatile、synchronized、Atomic 的区别？如果只有一个i++;的时候，volatile和synchronized能否互换？volatile作为修饰变量的时候，变量自加中途可能发生线程调度，volatile 可以保证在一个线程的工作内存中修改了该变量的值，该变量的值立即能回显到主内存中，从而保证所有的线程看到这个变量的值是一致的。但是有个前提，因为它不具有操作的原子性，也就是它不适合在对该变量的写操作依赖于变量本身自己。就比如i++、i+=1;这种。但是可以改为num=i+1;如果i是一个 volatile 类型，那么num就是安全的，总之就是不能作用于自身。synchronized是基于代码块的，只要包含在synchronized块中，就是线程安全的。AtomicInteger，一个轻量级的synchronized。使用的并不是同步代码块，而是Lock-Free算法(一个死循环调用了底层的比较方法直到相同后才退出循环)，最终的结果就是在高并发的时候，或者说竞争激烈的时候效率比synchronized高一些。ThreadLocal，线程中私有数据。主要用于线程改变内部的数据时不影响其他线程，使用时需要注意static。再补一个，才学到的。利用clone()方法，如果是一个类的多个对象想共用对象内部的一个变量，而又不想这个变量static，可以使用浅复制方式。(查看设计模式原型模式) HashTable、SynchronizedCollection、ConcurrentHashMap、Vector 静态内部类单例原理，静态内部类单例在反序列化时会重新生成新对象因此需要抛出一个异常，反序列化是比正常创建新对象慢的 compare and set (CAS)，不断重新取值比较，可能会出现ABA问题（中途值被修改又还原） Message全程持有发送该消息Handler的引用 直到Handler处理完消息，Message回收释放实际上经常会出现的Diaglog泄漏就是，Dialog销毁的时候Activity已经销毁，所以会发不出那个Handler git commit –amend 可以修改提交 onScroll和onFling的区别在于，onFline是用于滑动的最后一下 不要重复 setListener，要使用 v.getId 来复用 Listener，不然会创建一堆 Listener 导致频繁 GC final与static final的区别是：final在一个对象类唯一，static final在多个对象中都唯一；一个既是static又是final的域只占据一段不能改变的存储空间，只有一份。 View的UI刷新，不会导致阻塞的原因是View 的绘制与 Java 代码的looper无关，而是由底层 SurfaceFlinger 自身的事件处理机制处理的 因为不光是gui，同样的道理在几乎所有编程领域里都是这样的，这背后是线程同步的开销问题。显然两个线程不能同时draw，否则屏幕会花；不能同时insert map，否则内存会花；不能同时write buffer，否则文件会花。需要互斥，比如锁。结果就是同一时刻只有一个线程可以做ui。那么当两个线程互斥几率较大时，或者保证互斥的代码复杂时，选择其中一个长期持有其他发消息就是典型的解决方案。所以普遍的要求ui只能单线程 onChildDraw影响触摸事件的接受顺序，同样影响绘制顺序 对组件 Activity 而言，viewRootImpl 的初始化在 onCreate 之后，onResume 之后，故在onCreate里开子线程更新UI不会报错 Glide中into()同一View，但显示不同的图片时（url不同）是使用的同一个Bitmap引用地址，只是像素不一样，在onResourceReady()里可以打印得到的bitmap的地址是一样的。这是因为BitmapPool会复用Request，可能会拿到擦除过的bitmap bitmap对象是通过env-&gt;NewOject(…)，所以是分配在虚拟机中的 RecyclerView.OnScrollListener()来监听RecyclerView的滚动距离，正常情况下是没有问题的，但是一旦RecyclerView的数据源变了的时候，比如筛选条件变了，需要清除之前的数据，然后重新添加新的数据到RecyclerView里面去，这时候记录的滚动距离就不准了；自定义RecyclerView的LinearLayoutManager来计算滚动距离，但是在滑动的时候特别消耗性能，滑动卡顿 AppCompatActivity获得Activity的方法Android get hosting Activity from a view Java的正则式首先需要Pattern.compile()后必须要调用find()或者matches()才能找到 在自定义控件的构造函数或者其他绘制相关地方使用系统依赖的代码，会导致可视化编辑器无法报错并提示，使用isInEditMode()可以解决在 在Android Studio下的文件app.iml已经制定了assets文件夹的属性，所以直接新建Directory即可 view.layout(l,t,r,b)在子线程中没反应，需要post()或者onAnimationStart中 getGlobalVisibleRect(globalRect)在没有被遮挡（超出屏幕）时和getLocationInWindow数值相同; getDrawingRect(drawingRect)和getLocalVisibleRect(localRect)没有遮挡时测试数值相同都是相对自己的坐标 include设置了id，会覆盖掉layout_activity_head布局文件中根layout的id 全面屏刘海屏的状态栏是很高的，在使用getLocationInWindow时需要特别注意，需要减去的状态栏高度会更高 onCreate期间调用setHasOptionsMenu，则该fragment在其后的onCreateOptionsMenu回调可以接受到设置标题栏的menu 在selector的xml写法中，vector图标不能用android:color/transparent来表明空图像，因为颜色是没有高度的（instrictWidth），并且导致后续高宽不会自动更新 直接在selector的xml中设置item为透明 &lt;item android:drawable=&quot;@android:color/transparent&quot; android:state_checked=&quot;false&quot; /&gt; 的写法是错误的，会导致高宽都变化为0，并且高宽不会自动更新。应该更改vector的文件的颜色为android:fillColor=”#00000000” 设置Acticity为全屏会获得和adjustNothing一样的效果，不会顶起布局 viewstub被inflate后会消失，包括它的id，可以使用android:inflatedId=”@id/vs_bottom”来规定它被inflate后叫什么，这样可以防止viewStub消失后其他引用它的布局失去了依赖 rxjava中的timer也可以加入自己的scheduler viewstub的布局参数应当写到viewstub内，比如margin值，写在layout内是无效的 ,但是padding是可以写在layout内的；ViewStub中不能用merge android中setOnClickListener会自动设置view为clickable = true，所以自定义的simpleAdapter添加setOnItemClick的时候需要自动添加view的clickable = true may not a tips: GreenDao主键自增类型必须是Long，而不是long。否则你会发现id永远为0，永远只有一个数据 onStop可能不会被调用（eg. 新Acitivity采用了透明主题，当前Actiivty不会回调onStop） 回到原Activity onRestart -&gt; onStart -&gt; onResume 每个View都可以onSaveInstanceState，并且onSaveInstanceState及onRestoreInstanceState是和onStop及onStart绑定的，onSaveInstanceState在onStop前，onRestoreInstanceState在onStart后 onNewIntent()—-&gt;onResart()——&gt;onStart()—–&gt;onResume(). 且当调用到onNewIntent(intent)的时候，需要在onNewIntent() 中使用setIntent(intent)赋值给Activity的Intent.否则，后续的getIntent()都是得到老的Intent。 singleTask的Actiivty被启用时，在其上的栈顶都会被清空（默认具有FLAG_ACTIVITY_CLEAR_TOP）85.fitsSystemWindows=true要慎用，很多坑。比如WebView中输入框获取焦点弹出软键盘时出现抖动，还有哪个View设置了fitsSystemWindows=true软键盘弹出时哪个View就会被顶上去； WindowManager.LayoutParams.FLAG_LAYOUT_NO_LIMITS不要用，会导致EMUI3.1的系统下面虚拟按键挡住布局 事件传递 12345678bool dispatchTouchEvent(MotionEvent ev)&#123; bool consume = false; if(onInterceptTouchEvent(ev))&#123; consume = onTouchEvent(ev); &#125; else &#123; consume = child.dispatchTouchEvent(ev); &#125;&#125; FragmentStatePagerAdapter和FragmentPagerAdapter都没有从viewGroup中removeView，而是transaction中detach/ remove掉了， 因此viewPager在保存不是fragment的时候需要警惕，其他view并不会像fragment一样刷新 PathMeasure的forceClosed参数对绑定的Path不会产生任何影响，例如一个折线段的Path，本身是没有闭合的，forceClosed设置为True的时候，PathMeasure计算的Path是闭合的，但Path本身绘制出来是不会闭合的。 - 均摘自 gcssloop forceClosed参数对PathMeasure的测量结果有影响，还是例如前面说的一个折线段的Path，本身没有闭合，forceClosed设置为True，PathMeasure的计算就会包含最后一段闭合的路径，与原来的Path不同。 Math.atan2(tan[1],tan[0])解释：tan[0] = cos = 邻边(单位圆x坐标)，tan[1] = sin = 对边(单位圆y坐标)，因此可以计算任意两点间的斜率夹角 pre和post，左乘进行行变换，右乘进行列变换，pre表明M左乘pre，poste表明M右乘pre，M表明矩阵 由于硬件加速的问题，PathMeasure中的getSegment在讲Path添加到dst数组中时会被导致一些错误，需要通过mDst.lineTo(0,0)来避免这样一个Bug measure.getMatrix(distance, matrix, PathMeasure.TANGENT_MATRIX_FLAG | PathMeasure.POSITION_MATRIX_FLAG);这个方法是用于得到路径上某一长度的位置以及该位置的正切值的矩阵 透明主题的坑： 坑一：透明主题的 Activity 如果弹出键盘，并且是 adjustResize 模式，在键盘弹出的一瞬间可以看到前一个 Activity 原因：键盘弹起时，Activity 重新计算了高度缩短了，而键盘弹起有一个动画，在动画没有执行完毕之前，键盘所占用的空间上没有别的布局只有 Window ，而 Window 是透明的因此就看到了上一个 Activity 尝试的解决方案：在动画执行完毕后，将 Window 的背景设置为不透明，但是失败了，见坑二 坑二：如果当前的 Window 背景带有透明度，在动态改变背景的时候会闪一下屏，如果 Window 的初始背景颜色没有透明度，动态改变背景很完美 原因：不详，目测是 Android 的 bug 解决方案：无。。。 坑三：也是由解决坑二造成的，我试图通过监听键盘弹起事件来手动调节布局，这样 Activity 不需要 resize 也就没有那个 bug，但是发现监听键盘弹起的方法基本都是监听 Activity 重新布局后对比高度来判断的，因此在 adjustNothing 状态下无效，值得一提的是，跟三弟交流发现他们钻了一个空子，当 Activity 为全屏状态时 adjustResize 是不生效的，但是可以监听到键盘弹出，所以相当于是adjustNothing的效果，但这种情况没法在我这个项目使用。 对window.addFlags需要在setContentView前 sqlLight数据库not null只针对于text为null的情况，但是text为&quot;&quot;时是可以插入的。 数据库的unique属性的列，insert()一旦失败，则批量都失败。此时可以用insert or replace，不会影响批量中其他数据的插入。 setRetainInstance(true)设置为true，表示 configuration change 的时候，fragment实例不会背重新创建，即重建时不会经过onDestroy和onCreate。 fragment 该方法也常常被用来处理 Activity re-creation 时候数据的保存。因此可以干很多骚操作，但即使是使用setRetainInstance还是有一定概率会被回收的，重新创建fragment后，requestCode和callback的关系就没有了。 fragment 该方法也常常被用来处理 Activity re-creation 时候数据的保存。因此可以干很多骚操作，但即使是使用setRetainInstance还是有一定概率会被回收的，重新创建fragment后，requestCode和callback的关系就没有了。 SharedPreferences是文件的一种，读写有一定的缓存策略，在内存中会有一份SharedPreferences文件的缓存，因此在多进程模式下其读写就变得不可靠。当面对高并发读写访问的时候，有很大几率会丢失数据，故不建议在进程间通信中使用SharedPreferences Messenger是一种轻量级的IPC方案，它的底层实现就是AIDL。Messenger是以串行的方式处理请求的，即服务端只能一个个处理，不存在并发执行的情形 rxjava2不接受null，会报错 DialogFragment的出现解决AlertDialog和PopupWindow随屏幕切换而消失的问题 123Window window = getDialog().getWindow(); window.setLayout(ViewGroup.LayoutParams.MATCH_PARENT, ViewGroup.LayoutParams.MATCH_PARENT); 等效于 12345DisplayMetrics dm = new DisplayMetrics();getActivity().getWindowManager().getDefaultDisplay().getMetrics(dm);if (null != window) &#123; window.setLayout(dm.widthPixels, dm.heightPixels);&#125; fragment的commit和commitAllowingStateLoss的区别 onSaveInstanceState()再onStop()前调用，而再Honeycomb前，是在onPause()前立即调用的，因为Honeycomb前对于回收来说一不可见Activity就被kill了，Honeycomb后Activity要在onStop后才能被kill掉 参见Fragment Transactions &amp; Activity State Loss Android 代码proguard混淆之后的错误log查看方法 项目目录的progurad下有一个mapping.txt文件 这是混淆后的名字和原名字的映射关系。在ADT安装目录的sdk\tools\proguard\bin下有三个工具，其中proguardgui.bat就是用来还原的图形化工具。 6.0后需要动态申请权限，7.0后需要通过FileProvider访问其他的权限。6.0后的动态申请权限，有可能会出现未授权时录音，Android录音数据前几千个字节为空的问题。 多进程在进程被kill掉的时候来不及正常调用onDestroy等流程，此时会通过binder通知其他进程自己死掉了，见Binder死亡通知机制之linkToDeath Fragment的可见性：onHiddenChanged()只会在对fragment进行show/hide变换时调用，第一次进入的时候不会调用，在使用beginTransaction().hide(Fragment)会被调用，而且是在onResume之前；而在viewpager中的fragment则完全不一样，需要手动调用setUserVisible(boolean)。 注意onHiddenChanged的特殊情况 Android Fragment可见性的判断与监听完全实现 ViewGroup默认情况不会调用onDraw()，它在init中会被设置成WILL_NOT_DRAW，这是从性能考虑，这样一来，onDraw就不会被调用了。如果我们要重要一个ViweGroup的onDraw方法，有两种方法： 在构造函数里面，给其设置一个颜色，如#00000000。 在构造函数里面，调用setWillNotDraw(false)，去掉其WILL_NOT_DRAW flag110.可以使用.adjustViewBounds(true)方法来设置宽度铺满屏幕的ImageView，但是对于小于17的版本不能正确显示 fragment重叠的问题：只需要在onSaveInstanceState中置空fragmnet状态即可-》outState.putParcelable(“android:support:fragments”, null); drawable.getConstantState().newDrawable()使用的是相同ConstantState下新建了一个drawable，和getResources().getDrawable(R.drawable.xxx)返回的对象一样。所有从本地资源读取图片资源getResources().getDrawable(R.drawable.xxx)的drawable都共享ConstantState，对一个drawable做更改会影响其他drawable持有相同ConstantState的drawable，eg. 修改alpha。可以使用drawable.mutate()来创建一个不共享的drawable。 关于Drawable的缓存机制应该了解的知识 很悲伤的是Rxjava中DisposableSubscriber的onStart()是在subscribeOn指定的线程上进行的（通常是子线程），它在整个任务的开始的时候调用。而onComplete()、onNext()、onError()是在observeOn指定的线程上进行的（通常是UI线程）。事实上，如果不指定线程，observe都将在主线程进行 注意RecycleView里都每个子项设置focusable=true的时候，这个时候设置点击就可能被最后一个抢掉了。 当一个Activity反常销毁重建的时候，请先查看下你的“不保留活动“是不是开启了。 dp和px的关系，是与屏幕像素密度为160的倍数而不同：像素密度为160时1px = 1dp117.从AutoScroll的View说起Scroller的原理与作用二话不说，先看看判断RecyclerView是否滚动到底部中说到的computeVerticalScrollExtent()函数表示的View内容与View位置的相对滚动 更多应该阅读的 CopyOnWriteArrayList ThreadPoolExecutor WeakHashMap Activity的SingleTask模式，使得onIntent可用 notifyItemChanged，因为DataSetChange可能会造成闪烁 Producer Extends Consumer Super &lt;? extends Fruits&gt; &lt;? super Apple&gt; 基于Window.ID_ANDROID_CONTENT给定id添加子View ArrayList ，LinkedList不同步 Vector同步 HashSet，LinkedHashSet,HashMap 不同步 HashTable 同步 同步的ArrayList = Collections.synchronizedList(new ArrayList(…)); SpannableString 数据库索引失败 Encapsulate Collection 学习JEE规范，去看JBoss源码；学习类加载器，就去看OSGI源码]]></content>
      <categories>
        <category>Android</category>
      </categories>
      <tags>
        <tag>Android</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[如何快速搭建个人博客]]></title>
    <url>%2F2019-01-22-%E5%A6%82%E4%BD%95%E5%BF%AB%E9%80%9F%E6%90%AD%E5%BB%BA%E4%B8%AA%E4%BA%BA%E5%8D%9A%E5%AE%A2.html</url>
    <content type="text"><![CDATA[记录搜集到的github Page搭建方法～ 首先如何快速搭成一个模型.. 利用 GitHub Pages 快速搭建个人博客 如果使用jekyll s命令调试本地搭建的博客，需要注意原作者在github上的一句话 哪里去找后续应该如何改动呢？ 打造个性超赞博客Hexo+NexT+GitHubPages的超深度优化 【持续更新】最全Hexo博客搭建+主题优化+插件配置+常用操作+错误分析 Github+hexo+next主题博客搭建教程]]></content>
      <categories>
        <category>小白教程</category>
      </categories>
      <tags>
        <tag>搭建博客</tag>
      </tags>
  </entry>
</search>
